var __index = {"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"index.html","level":1,"title":"Home","text":"<p>Generate conda packages directly from pure python wheels</p> <p>whl2conda is a command line utility for building and testing  conda packages generated directly from pure python wheels.</p> <p>Project Background</p> <p>See Motivation for the story behind this project.</p>","path":["Home"],"tags":[]},{"location":"index.html#features","level":2,"title":"Features","text":"<ul> <li> <p>Performance: because it does not need to create conda environments     in order to build, this is much faster than solutions involving conda-build.</p> </li> <li> <p>Dependency renaming: renames pypi package dependencies to their      corresponding conda name. Automatically renames packages from known     list collected from conda-forge and supports user-specified rename     patterns as well.</p> </li> <li> <p>Multiple package formats: can generate both V1 ('.tar.bz2') and V2 ('.conda')     conda package formats. Can also generate an unpacked directory tree for debugging     or additional user customization.</p> </li> <li> <p>Project configuration: whl2conda project-specific options can be read from     project's <code>pyproject.toml</code> file.</p> </li> <li> <p>Test install support: supports installing conda package into a conda     environment for testing prior to deployment.</p> </li> <li> <p>Hides pypi dependencies: rewrites the original pip/pypi dependencies in the     installed dist-info to avoid compatibility issues.</p> </li> </ul>","path":["Home"],"tags":[]},{"location":"install.html","level":1,"title":"Install","text":"pipcondamambapixi <pre><code>pip install whl2conda\n</code></pre> <pre><code>conda install -c conda-forge whl2conda\n</code></pre> <pre><code>mamba install -c conda-forge whl2conda\n</code></pre> <pre><code>pixi workspace channel add conda-forge\npixi add whl2conda\n</code></pre>","path":["Install"],"tags":[]},{"location":"install.html#prerequisites","level":2,"title":"Prerequisites","text":"<p>It is assumed that you have installed conda, and that it is in the program path, but it is currently only required for <code>whl2conda install</code>. Furthermore, if you use the <code>--conda-bld</code> option, you must have <code>conda-index</code> installed in your base environment (you will already have it if you have <code>conda-build</code>).</p>","path":["Install"],"tags":[]},{"location":"about/changelog.html","level":1,"title":"whl2conda changes","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2670-2026-7-11","level":2,"title":"[26.7.0] - 2026-7-11","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#features","level":3,"title":"Features","text":"<ul> <li>Convert stable ABI (abi3) wheels into a single conda package usable on   all supported python versions, following the CEP-20 layout: the python   dependency is only a lower bound, and files are installed using the   noarch python machinery (#183)</li> <li>The <code>--python</code> option now overrides the automatically generated python   version pin for binary (<code>--allow-impure</code>) conversions (#183)</li> <li>New <code>--for-conda-forge</code> option (synonym: <code>--for-cpython</code>) adds the CEP-20   python pins used by conda-forge (<code>cpython</code> and <code>_python_abi3_support</code>)   when converting stable ABI (abi3) wheels (#194)</li> <li><code>whl2conda diff</code> now semantically analyzes the differences between two   conda packages by default, reporting notable and unexpected differences   while suppressing differences expected between whl2conda-generated and   recipe-built packages. The <code>-T</code>/<code>--diff-tool</code> option is now optional and   selects the previous raw diff behavior. The analysis engine is also   available programmatically as <code>whl2conda.api.compare</code>.</li> <li>New <code>--platform-tag</code> option selects the wheel platform tag to convert   for when converting a multi-platform (\"fat\") binary wheel (#201)</li> <li>New <code>--all-platforms</code> option generates a conda package for every   platform supported by the wheel, each written into a <code>&lt;subdir&gt;/</code>   subdirectory of the output directory (#204)</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#changes","level":3,"title":"Changes","text":"<ul> <li>Binary wheel conversion (<code>--allow-impure</code>) is no longer labeled   experimental: conversions of representative C, C++, Cython, rust, and   abi3 extension packages are verified to be semantically equivalent to   the corresponding conda-forge packages on linux, macOS, and windows by   the new comparison suite. The converter now emits a targeted warning   when a wheel bundles vendored shared libraries (the main remaining   limitation) instead of a blanket experimental warning on every binary   conversion.</li> <li><code>whl2conda install</code> no longer checks the conda-libmamba-solver version   or switches target environments to the classic solver. That workaround   was only needed for conda-libmamba-solver older than 24.1.0, which fixed   the underlying file-install issue   (conda-libmamba-solver#418).   This also removes a crash when conda-libmamba-solver was not installed   in the base environment.</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#development","level":3,"title":"Development","text":"<ul> <li>Documentation is now generated with Zensical   instead of the no-longer-maintained MkDocs/mkdocs-material (#173)</li> <li>New external comparison test suite (<code>pixi run compare-conda-forge</code>)   converts a curated sample of binary PyPI packages and semantically   compares the results against the corresponding conda-forge packages,   building evidence toward removing the experimental label from binary   conversion. Supersedes the <code>research/compare_packages.py</code> prototype,   which has been removed.</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#bug-fixes","level":3,"title":"Bug fixes","text":"<ul> <li>The <code>timestamp</code> in the package's <code>index.json</code> is now written in   milliseconds since the epoch, matching conda-build and rattler-build.   It was previously written in seconds and was also incorrectly shifted   by the local timezone offset. (#193)</li> <li><code>whl2conda diff</code> no longer fails on packages that do not contain an   <code>info/files</code> entry, e.g. packages built by rattler-build (#192)</li> <li>Packages converted on Windows no longer contain backslash path   separators in <code>info/paths.json</code> and <code>info/files</code>, which violated   the conda package format (#203)</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2621-2026-7-9","level":2,"title":"[26.2.1] - 2026-7-9","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#bug-fixes_1","level":3,"title":"Bug fixes","text":"<ul> <li>Dependency names that do not match any rename rule are again passed through   with their original spelling, instead of being rewritten to their PEP 503   normalized form. This restores pre-26.2.0 behavior for packages whose conda   name contains <code>_</code>, <code>.</code> or uppercase characters. (#184)</li> <li>Dependency rename patterns are now matched against both the name as written   in the wheel metadata and its PEP 503 normalized form, so patterns   containing <code>_</code> or <code>.</code> work again. (#184)</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#development_1","level":3,"title":"Development","text":"<ul> <li>Automated release workflow: pushing a version tag publishes to PyPI   (trusted publishing), creates the GitHub release, and deploys the   versioned documentation (#145)</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2620-2026-2-22","level":2,"title":"[26.2.0] - 2026-2-22","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#features_1","level":3,"title":"Features","text":"<ul> <li>Experimental support for converting binary (non-pure Python) wheels via <code>--allow-impure</code> flag</li> <li>Support parsing name from Poetry 2.0 pyproject.toml</li> <li>Handle bad UTF-8 in METADATA files</li> <li>Support wheel metadata version 2.5 (PEP 770) - SBOM documents</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#bug-fixes_2","level":3,"title":"Bug fixes","text":"<ul> <li>Normalize PyPI package names per PEP 503 when applying dependency renames (#134)</li> <li>Show user-friendly error message when wheel download from PyPI fails (#125)</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#changes_1","level":3,"title":"Changes","text":"<ul> <li>Drop support for python 3.9</li> <li>Internal code refactoring and cleanup</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#development_2","level":3,"title":"Development","text":"<ul> <li>Switched to pixi for development environment management</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2530-2025-3-20","level":2,"title":"[25.3.0] - 2025-3-20","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#features_2","level":3,"title":"Features","text":"<ul> <li>Support metadata version 2.4 (PEP 688)</li> <li>Added --allow-metadata-version option to convert command</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#changes_2","level":3,"title":"Changes","text":"<ul> <li>Drops support for python 3.8</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2450-2024-5-5","level":2,"title":"[24.5.0] - 2024-5-5","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#features_3","level":3,"title":"Features","text":"<ul> <li>Added persistent user settings for:<ul> <li>default conda format</li> <li>whether to automatically update stdrenames table</li> <li>specify aliases for extra pypi indexes</li> </ul> </li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2440-2024-4-14","level":2,"title":"[24.4.0] - 2024-4-14","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#changes_3","level":3,"title":"Changes","text":"<ul> <li>Only use classic installer in <code>whl2conda install</code> environments if      <code>conda-libmamba-solver</code> in base environment has version less than 24.1.0 (see #118)</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#bug-fixes_3","level":3,"title":"Bug fixes","text":"<ul> <li>Transfer executable file permissions from wheel (#135)</li> <li>Correct typos in documentation.</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2412-2024-1-28","level":2,"title":"[24.1.2] - 2024-1-28","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#features_4","level":3,"title":"Features","text":"<ul> <li>Support <code>whl2conda convert --python</code> override.</li> <li>Added one-line description to whl2conda package</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2411-2024-1-15","level":2,"title":"[24.1.1] - 2024-1-15","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#features_5","level":3,"title":"Features","text":"<ul> <li>Support conversion of wheels containing <code>*.data</code> directories</li> <li>Support direct download of wheels to convert from pypi repositories</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2410-2024-1-8","level":2,"title":"[24.1.0] - 2024-1-8","text":"","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#features_6","level":3,"title":"Features","text":"<ul> <li>Add <code>whl2conda build</code> - limited experimental drop-in replacement for <code>conda build</code></li> <li>Support translation of <code>~=</code> and <code>===</code> version specification operators.</li> <li><code>whl2conda install</code> now supports installing multiple package files at once</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#bug-fixes_4","level":3,"title":"Bug fixes","text":"<ul> <li>Use classic installer in <code>whl2conda install</code> environments to work around conda bug (#114)</li> <li>Include project URLs in metadata that have multi-word keys (#113)</li> <li>Write METADATA file in dist-info using UTF8 (#112)</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2390-2023-9-23","level":2,"title":"[23.9.0] - 2023-9-23","text":"<ul> <li>First official stable release</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#23810-2023-9-16-prerelease","level":2,"title":"[23.8.10] - 2023-9-16 (prerelease)","text":"<ul> <li>Fix generation of entry points</li> <li>Adjust metadata generation</li> <li>Add <code>whl2conda diff</code> subcommand</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2389-2023-9-14-prerelease","level":2,"title":"[23.8.9] - 2023-9-14 (prerelease)","text":"<ul> <li>Support <code>python -m whl2conda</code></li> <li>Fix issue with license copying</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2388-2023-9-10-prerelease","level":2,"title":"[23.8.8] - 2023-9-10 (prerelease)","text":"<ul> <li>hide pip build wheel output with <code>--quiet</code></li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/changelog.html#2387-2023-9-9-prerelease","level":2,"title":"[23.8.7] - 2023-9-9 (prerelease)","text":"<ul> <li>first conda-forge release</li> </ul>","path":["About","whl2conda changes"],"tags":[]},{"location":"about/license.html","level":1,"title":"License","text":"<pre><code>                             Apache License\n                       Version 2.0, January 2004\n                    http://www.apache.org/licenses/\n</code></pre> <p>TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION</p> <ol> <li> <p>Definitions.</p> <p>\"License\" shall mean the terms and conditions for use, reproduction,   and distribution as defined by Sections 1 through 9 of this document.</p> <p>\"Licensor\" shall mean the copyright owner or entity authorized by   the copyright owner that is granting the License.</p> <p>\"Legal Entity\" shall mean the union of the acting entity and all   other entities that control, are controlled by, or are under common   control with that entity. For the purposes of this definition,   \"control\" means (i) the power, direct or indirect, to cause the   direction or management of such entity, whether by contract or   otherwise, or (ii) ownership of fifty percent (50%) or more of the   outstanding shares, or (iii) beneficial ownership of such entity.</p> <p>\"You\" (or \"Your\") shall mean an individual or Legal Entity   exercising permissions granted by this License.</p> <p>\"Source\" form shall mean the preferred form for making modifications,   including but not limited to software source code, documentation   source, and configuration files.</p> <p>\"Object\" form shall mean any form resulting from mechanical   transformation or translation of a Source form, including but   not limited to compiled object code, generated documentation,   and conversions to other media types.</p> <p>\"Work\" shall mean the work of authorship, whether in Source or   Object form, made available under the License, as indicated by a   copyright notice that is included in or attached to the work   (an example is provided in the Appendix below).</p> <p>\"Derivative Works\" shall mean any work, whether in Source or Object   form, that is based on (or derived from) the Work and for which the   editorial revisions, annotations, elaborations, or other modifications   represent, as a whole, an original work of authorship. For the purposes   of this License, Derivative Works shall not include works that remain   separable from, or merely link (or bind by name) to the interfaces of,   the Work and Derivative Works thereof.</p> <p>\"Contribution\" shall mean any work of authorship, including   the original version of the Work and any modifications or additions   to that Work or Derivative Works thereof, that is intentionally   submitted to Licensor for inclusion in the Work by the copyright owner   or by an individual or Legal Entity authorized to submit on behalf of   the copyright owner. For the purposes of this definition, \"submitted\"   means any form of electronic, verbal, or written communication sent   to the Licensor or its representatives, including but not limited to   communication on electronic mailing lists, source code control systems,   and issue tracking systems that are managed by, or on behalf of, the   Licensor for the purpose of discussing and improving the Work, but   excluding communication that is conspicuously marked or otherwise   designated in writing by the copyright owner as \"Not a Contribution.\"</p> <p>\"Contributor\" shall mean Licensor and any individual or Legal Entity   on behalf of whom a Contribution has been received by Licensor and   subsequently incorporated within the Work.</p> </li> <li> <p>Grant of Copyright License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       copyright license to reproduce, prepare Derivative Works of,       publicly display, publicly perform, sublicense, and distribute the       Work and such Derivative Works in Source or Object form.</p> </li> <li> <p>Grant of Patent License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       (except as stated in this section) patent license to make, have made,       use, offer to sell, sell, import, and otherwise transfer the Work,       where such license applies only to those patent claims licensable       by such Contributor that are necessarily infringed by their       Contribution(s) alone or by combination of their Contribution(s)       with the Work to which such Contribution(s) was submitted. If You       institute patent litigation against any entity (including a       cross-claim or counterclaim in a lawsuit) alleging that the Work       or a Contribution incorporated within the Work constitutes direct       or contributory patent infringement, then any patent licenses       granted to You under this License for that Work shall terminate       as of the date such litigation is filed.</p> </li> <li> <p>Redistribution. You may reproduce and distribute copies of the       Work or Derivative Works thereof in any medium, with or without       modifications, and in Source or Object form, provided that You       meet the following conditions:</p> <p>(a) You must give any other recipients of the Work or       Derivative Works a copy of this License; and</p> <p>(b) You must cause any modified files to carry prominent notices       stating that You changed the files; and</p> <p>© You must retain, in the Source form of any Derivative Works       that You distribute, all copyright, patent, trademark, and       attribution notices from the Source form of the Work,       excluding those notices that do not pertain to any part of       the Derivative Works; and</p> <p>(d) If the Work includes a \"NOTICE\" text file as part of its       distribution, then any Derivative Works that You distribute must       include a readable copy of the attribution notices contained       within such NOTICE file, excluding those notices that do not       pertain to any part of the Derivative Works, in at least one       of the following places: within a NOTICE text file distributed       as part of the Derivative Works; within the Source form or       documentation, if provided along with the Derivative Works; or,       within a display generated by the Derivative Works, if and       wherever such third-party notices normally appear. The contents       of the NOTICE file are for informational purposes only and       do not modify the License. You may add Your own attribution       notices within Derivative Works that You distribute, alongside       or as an addendum to the NOTICE text from the Work, provided       that such additional attribution notices cannot be construed       as modifying the License.</p> <p>You may add Your own copyright statement to Your modifications and   may provide additional or different license terms and conditions   for use, reproduction, or distribution of Your modifications, or   for any such Derivative Works as a whole, provided Your use,   reproduction, and distribution of the Work otherwise complies with   the conditions stated in this License.</p> </li> <li> <p>Submission of Contributions. Unless You explicitly state otherwise,       any Contribution intentionally submitted for inclusion in the Work       by You to the Licensor shall be under the terms and conditions of       this License, without any additional terms or conditions.       Notwithstanding the above, nothing herein shall supersede or modify       the terms of any separate license agreement you may have executed       with Licensor regarding such Contributions.</p> </li> <li> <p>Trademarks. This License does not grant permission to use the trade       names, trademarks, service marks, or product names of the Licensor,       except as required for reasonable and customary use in describing the       origin of the Work and reproducing the content of the NOTICE file.</p> </li> <li> <p>Disclaimer of Warranty. Unless required by applicable law or       agreed to in writing, Licensor provides the Work (and each       Contributor provides its Contributions) on an \"AS IS\" BASIS,       WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or       implied, including, without limitation, any warranties or conditions       of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A       PARTICULAR PURPOSE. You are solely responsible for determining the       appropriateness of using or redistributing the Work and assume any       risks associated with Your exercise of permissions under this License.</p> </li> <li> <p>Limitation of Liability. In no event and under no legal theory,       whether in tort (including negligence), contract, or otherwise,       unless required by applicable law (such as deliberate and grossly       negligent acts) or agreed to in writing, shall any Contributor be       liable to You for damages, including any direct, indirect, special,       incidental, or consequential damages of any character arising as a       result of this License or out of the use or inability to use the       Work (including but not limited to damages for loss of goodwill,       work stoppage, computer failure or malfunction, or any and all       other commercial damages or losses), even if such Contributor       has been advised of the possibility of such damages.</p> </li> <li> <p>Accepting Warranty or Additional Liability. While redistributing       the Work or Derivative Works thereof, You may choose to offer,       and charge a fee for, acceptance of support, warranty, indemnity,       or other liability obligations and/or rights consistent with this       License. However, in accepting such obligations, You may act only       on Your own behalf and on Your sole responsibility, not on behalf       of any other Contributor, and only if You agree to indemnify,       defend, and hold each Contributor harmless for any liability       incurred by, or claims asserted against, such Contributor by reason       of your accepting any such warranty or additional liability.</p> </li> </ol> <p>END OF TERMS AND CONDITIONS</p> <p>APPENDIX: How to apply the Apache License to your work.</p> <pre><code>  To apply the Apache License to your work, attach the following\n  boilerplate notice, with the fields enclosed by brackets \"{}\"\n  replaced with your own identifying information. (Don't include\n  the brackets!)  The text should be enclosed in the appropriate\n  comment syntax for the file format. We also recommend that a\n  file or class name and description of purpose be included on the\n  same \"printed page\" as the copyright notice for easier\n  identification within third-party archives.\n</code></pre> <p>Copyright 2023-2024   Christopher Barber</p> <p>Licensed under the Apache License, Version 2.0 (the \"License\");    you may not use this file except in compliance with the License.    You may obtain a copy of the License at</p> <pre><code>   http://www.apache.org/licenses/LICENSE-2.0\n</code></pre> <p>Unless required by applicable law or agreed to in writing, software    distributed under the License is distributed on an \"AS IS\" BASIS,    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.    See the License for the specific language governing permissions and    limitations under the License.</p>","path":["About","License"],"tags":[]},{"location":"about/license.html#additional-licenses","level":2,"title":"Additional licenses","text":"<p>This source code incorporates some code taken from other projects with compatible licenses, specifically:</p> <ul> <li>https://github.com/conda/conda/blob/main/conda/models/version.py (BSD-3)</li> </ul>","path":["About","License"],"tags":[]},{"location":"about/motivation.html","level":1,"title":"Motivation","text":"","path":["About","Motivation"],"tags":[]},{"location":"about/motivation.html#the-problem","level":2,"title":"The Problem","text":"<p>My team relied on conda for managing Python environments and distributing packages across our organization. While conda excelled at dependency management, we encountered a significant bottleneck: building conda packages was painfully slow.</p> <p>For large projects with multiple interdependent packages, builds could take 30 minutes or more. Most of this time wasn't spent on actual compilation—our pure-Python packages required no building at all. Instead, conda-build spent the bulk of its time setting up isolated conda environments just to run <code>pip install</code> and copy files.</p>","path":["About","Motivation"],"tags":[]},{"location":"about/motivation.html#the-insight","level":2,"title":"The Insight","text":"<p>This inefficiency led me to examine what conda-build actually does for pure-Python packages. I discovered that it simply installs the wheel into a temporary environment and packages the result. All the necessary metadata— dependencies, entry points, version information—was already present in the wheel files we were building for our internal PyPI server.</p> <p>A key question emerged: What if we could generate conda packages directly from wheels, bypassing conda-build entirely?</p> <p>After studying the specifications for both wheel and conda package formats, I realized this was not only feasible but relatively straightforward. The main technical challenge was dependency translation: PyPI and conda-forge use different package names, so dependencies needed to be renamed during conversion.</p>","path":["About","Motivation"],"tags":[]},{"location":"about/motivation.html#the-solution","level":2,"title":"The Solution","text":"<p>I built whl2conda to convert wheels directly into conda packages, extracting all necessary metadata from the wheel itself. This eliminates two major pain points:</p> <ol> <li> <p>No conda recipe files needed. With traditional conda-build, you must    write and maintain <code>meta.yaml</code> recipe files that duplicate information    already present in your Python package. whl2conda extracts everything it    needs from the wheel—dependencies, entry points, version information—so    there's no separate recipe to write or keep in sync.</p> </li> <li> <p>Automatic dependency translation. PyPI and conda-forge use different    package names, so dependencies must be renamed during conversion. We had    previously solved this using hand-maintained YAML files, but I discovered    that conda-forge itself maintains a comprehensive mapping database. By    leveraging this resource, whl2conda handles dependency renaming    automatically for thousands of packages.</p> </li> </ol> <p>The result is a lightweight tool that converts pure-Python wheels to conda packages in seconds. No conda environments are needed for conversion, eliminating the primary bottleneck. The tool's own dependencies are minimal, and you only need to create a conda environment when testing the resulting packages.</p>","path":["About","Motivation"],"tags":[]},{"location":"about/motivation.html#additional-benefits","level":2,"title":"Additional Benefits","text":"<p>Beyond building our own packages, we discovered another valuable use case: converting existing PyPI packages into conda packages for our internal repositories. While pip packages can be installed into conda environments easily, conda packages cannot express dependencies on PyPI-only packages. This creates a gap when you need reliable, reproducible conda environments.</p> <p>Converting PyPI packages to conda format solved this problem. We could create conda versions of useful libraries—especially those no longer actively maintained or whose maintainers had no interest in conda-forge distribution—and host them in our internal channels. This gave us full control over our dependency stack while maintaining conda's environment management benefits.</p>","path":["About","Motivation"],"tags":[]},{"location":"about/motivation.html#broader-applications","level":2,"title":"Broader Applications","text":"<p>whl2conda began as an open-source hobby project to solve my team's specific pain points. We've successfully used it to eliminate conda-build from our packaging workflows, dramatically reducing build and deployment times.</p> <p>The tool may benefit anyone who needs to build conda packages for enterprise repositories or private channels. It could also benefit conda-forge itself: converting existing wheels would be far cheaper than rebuilding packages from source, potentially reducing infrastructure costs for the community.</p> <p>—Christopher Barber, creator of whl2conda</p>","path":["About","Motivation"],"tags":[]},{"location":"about/support.html","level":1,"title":"Support","text":"<ul> <li>Ask a question</li> <li>Look at open issues (before opening a bug or feature request)</li> <li>Report a bug</li> <li>Make a feature request</li> <li>Use the Source, Luke...</li> </ul>","path":["About","Support"],"tags":[]},{"location":"guide/basic-usage.html","level":1,"title":"Basic usage","text":"","path":["User Guide","Basic usage"],"tags":[]},{"location":"guide/basic-usage.html#converting-an-existing-wheel","level":2,"title":"Converting an existing wheel","text":"<p>To convert an existing Python wheel to a conda package, you only need to  run <code>whl2conda convert</code>:</p> <pre><code>$ whl2conda convert mypackage-1.2.3-py3-none-any.whl\nReading mypackage-1.2.3-py3-none-any.whl\nWriting mypackage-1.2.3-py_0.conda\n</code></pre> <p>By default, this will create the conda package in the same directory as the wheel. You can specify a different output directory using the <code>--out-dir</code>, e.g.:</p> <pre><code>$ whl2conda convert mypackage-1.2.3-py3-none-any.whl --out-dir conda-dist\n</code></pre> <p>This will automatically convert pypi package dependencies to their corresponding conda packages, renaming them as needed. Standard pypi/conda name mappings are taken from conda-forge. This will do the right thing for most packages, but you may also need to provide your own dependency rules to modify the dependencies. This is described in the section on Dependency Modification.</p>","path":["User Guide","Basic usage"],"tags":[]},{"location":"guide/basic-usage.html#downloading-wheels","level":2,"title":"Downloading wheels","text":"<p>If you want to create a conda package for a pure python package from pypi that doesn't currently have one available on a public channel, you can download the wheel using pip download. You do not need to download dependencies and  want a wheel, not an sdist, so use:</p> <pre><code>$ pip download --only-binary:all: --no-deps &lt;some-package-spec&gt;\n</code></pre> <p>Then you can convert the downloaded wheel using <code>whl2conda convert</code>.</p> <p>Or you can use either the <code>--from-pypi</code> or <code>--from-index</code> options to <code>whl2conda convert</code> to do this download for you, for example:</p> <pre><code>$ whl2conda convert --from-pypi 'some-package ==1.2.3'\n</code></pre> <p>The <code>--from-index</code> option expects either the full URL of the pypi index to download from or an alias, which may either be taken from a repository entry in your ~/.pypirc file or from an entry in the user's persistent whl2conda configuration. For instance, you could register a new index using a command like:</p> <p><pre><code>$ whl2conda config --set pypi-index.myindex https://myindex.com/pypi/\n</code></pre> and then convert using:</p> <pre><code>$ whl2conda convert --from-index myindex 'some-package'\n</code></pre>","path":["User Guide","Basic usage"],"tags":[]},{"location":"guide/basic-usage.html#building-from-project-directories","level":2,"title":"Building from project directories","text":"<p>If you are creating a conda package for your own python project that uses either a <code>pyproject.toml</code> file or a <code>setup.py</code> file, you can specify the project directory instead of a wheel file, or you can omit the positional argument if the project is located in the current working directory.</p> <pre><code>$ whl2conda convert my-project/\n</code></pre> <p>When run this way, there must be a <code>pyproject.toml</code> or <code>setup.py</code> file  in the specified directory. If there is a <code>pyproject.toml</code> file, whl2conda will read any project-specific options from the <code>[tool.whl2conda]</code> section (see Configuring pyproject.toml for details) and will then look for a wheel in the <code>dist/</code> subdirectory of the project.</p> <p>If there is only one <code>.whl</code> file in the <code>dist/</code> directory, that will be used. Otherwise, if there is an interactive terminal and the  <code>--batch</code> option has not been specified, whl2conda will prompt the user to choose a wheel or to build one using pip wheel.</p> <p>You can use the <code>--build-wheel</code> option to force the wheel to be built without prompting. So you can build both your project's wheel and a conda package non-interactively using the command:</p> <pre><code>$ whl2conda convert my-project/ --build-wheel --batch --yes\n</code></pre>","path":["User Guide","Basic usage"],"tags":[]},{"location":"guide/basic-usage.html#output-formats","level":2,"title":"Output formats","text":"<p>By default, <code>whl2conda convert</code> will generate a V2 format file with a <code>.conda</code> extension.  If you instead want the old V1 format, which uses the <code>.tar.bz2</code> extension, you can use the <code>--format</code> option:</p> <pre><code>$ whl2conda convert mypackage-1.2.3-py3-none-any.whl --format V1\nReading mypackage-1.2.3-py3-none-any.whl\nWriting mypackage-1.2.3-py_0.tar.bz2\n</code></pre> <p>You can change the default output format through a persistent user setting, e.g.:</p> <pre><code>$ whl2conda config --set conda-format V1\n</code></pre> <p>You can also specify the format <code>tree</code> to generate the conda package as a directory tree, so that you can examine its contents for debugging purposes.</p>","path":["User Guide","Basic usage"],"tags":[]},{"location":"guide/binary-conversion.html","level":1,"title":"Binary Wheel Conversion","text":"","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#overview","level":2,"title":"Overview","text":"<p>By default, whl2conda converts pure Python wheels (<code>py3-none-any</code>) into <code>noarch: python</code> conda packages. With the <code>--allow-impure</code> flag, it can also convert platform-specific binary wheels containing compiled extensions (<code>.so</code>, <code>.pyd</code>, <code>.dll</code>) into architecture-specific conda packages.</p> <pre><code>whl2conda convert --allow-impure markupsafe-3.0.2-cp312-cp312-manylinux_2_17_x86_64.whl\n</code></pre> <p>This produces a conda package with:</p> <ul> <li>Platform-specific subdir (e.g. <code>linux-64</code>, <code>osx-arm64</code>, <code>win-64</code>)</li> <li>Tight Python version pin (e.g. <code>python &gt;=3.12,&lt;3.13.0a0</code>)</li> <li>Python ABI constraint (e.g. <code>python_abi 3.12.* *_cp312</code>)</li> <li>OS minimum version constraints for macOS wheels (e.g. <code>__osx &gt;=11.0</code>)</li> </ul> <p>You can override the automatically derived Python pin with the <code>--python</code> option, in which case no <code>python_abi</code> constraint is added:</p> <pre><code>whl2conda convert --allow-impure --python '&gt;=3.12' some-wheel.whl\n</code></pre>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#stable-abi-abi3-wheels","level":2,"title":"Stable ABI (abi3) wheels","text":"<p>Wheels built against the CPython stable ABI (with an <code>abi3</code> tag, e.g. <code>cp312-abi3-macosx_11_0_arm64</code>) work on the tagged Python version and all later versions. whl2conda recognizes the <code>abi3</code> tag and produces a single platform-specific conda package that installs on any compatible Python:</p> <ul> <li>The Python dependency is only a floor (e.g. <code>python &gt;=3.12</code>) derived from   the wheel's Python tag; no upper bound or <code>python_abi</code> constraint is added.   A tighter <code>Requires-Python</code> constraint from the wheel metadata is kept.</li> <li>The package keeps its platform subdir (e.g. <code>osx-arm64</code>) but is marked   <code>noarch: python</code> and lays its files out under <code>site-packages/</code>, so the   installer relocates them into the environment's actual <code>site-packages</code>   directory regardless of Python version. This follows the conda ecosystem   convention for abi3 packages   (CEP-20).</li> <li>The build string is marked accordingly, e.g. <code>py312_abi3_0</code>.</li> </ul> <p>Note</p> <p>This package layout is supported by all current conda install tools — the <code>noarch: python</code> install machinery it relies on has been present since conda 4.3 (2017) and works regardless of the package's platform subdir, as codified by CEP-20.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#conda-forge-style-pins","level":3,"title":"conda-forge style pins","text":"<p>By default the python dependency is a plain floor (e.g. <code>python &gt;=3.12</code>), which is solvable on any channel. conda-forge instead pins its abi3 packages with <code>cpython &gt;=3.12</code> (which excludes PyPy, whose C API layer does not support the stable ABI) and <code>_python_abi3_support 1.*</code> (which additionally excludes free-threaded python builds). Pass <code>--for-conda-forge</code> (or its synonym <code>--for-cpython</code>) to add these pins:</p> <pre><code>whl2conda convert mypackage-1.0-cp312-abi3-macosx_11_0_arm64.whl \\\n    --allow-impure --for-conda-forge\n</code></pre> <p>Since the <code>cpython</code> and <code>_python_abi3_support</code> packages only exist on the conda-forge channel, do not use this option for packages targeting channels without them (e.g. Anaconda's <code>defaults</code>).</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#downloading-binary-wheels","level":2,"title":"Downloading binary wheels","text":"<p>You can download and convert binary wheels from PyPI in a single step using the <code>--from-pypi</code> flag together with the download target options:</p> <pre><code>whl2conda convert --from-pypi markupsafe \\\n    --download-platform manylinux2014_x86_64 \\\n    --download-python-version 3.12 \\\n    --download-abi cp312\n</code></pre> <p>Using <code>--download-platform</code> automatically implies <code>--allow-impure</code>.</p> <p>The download target options are:</p> Option Description Example <code>--download-platform</code> Wheel platform tag <code>manylinux2014_x86_64</code>, <code>macosx_11_0_arm64</code>, <code>win_amd64</code> <code>--download-python-version</code> Target Python version <code>3.12</code> <code>--download-abi</code> Target ABI tag <code>cp312</code> <p>These options work with both <code>--from-pypi</code> and <code>--from-index</code> and are passed through to <code>pip download</code>.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#cross-platform-downloads","level":3,"title":"Cross-platform downloads","text":"<p>You can download wheels for a different platform than the one you are running on. For example, on macOS you can download and convert a Linux wheel:</p> <pre><code>whl2conda convert --from-pypi ujson \\\n    --download-platform manylinux2014_x86_64 \\\n    --download-python-version 3.12 \\\n    --download-abi cp312 \\\n    --out-dir conda-dist/\n</code></pre> <p>The target platform is determined by the wheel's platform tag, not the host OS. To produce conda packages for multiple platforms, download and convert the appropriate wheel for each one.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#environment-markers","level":2,"title":"Environment markers","text":"<p>When converting binary wheels, whl2conda evaluates platform-specific environment markers on dependencies against the target platform. For example, if a Linux wheel declares:</p> <pre><code>nvidia-cuda-runtime-cu12 &gt;=12.0; platform_system == \"Linux\"\n</code></pre> <p>this dependency will be included in the converted conda package because the marker matches the wheel's target platform. Dependencies whose markers do not match the target (e.g. a Windows-only dependency in a Linux wheel) are excluded.</p> <p>For pure Python (noarch) conversions, dependencies with environment markers are still skipped, since noarch packages are platform-independent.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#what-works-well","level":2,"title":"What works well","text":"<p>Binary conversion is suited for self-contained extension packages, where all compiled code (including any statically linked libraries) is bundled in the wheel and the package's runtime dependencies are available as conda packages. The comparison suite described below verifies — on linux, macOS, and windows — that converted packages for all of the following kinds of wheels are semantically equivalent to the corresponding conda-forge packages:</p> <ul> <li>C/C++ extensions — e.g. markupsafe, wrapt, ujson, psutil</li> <li>Cython extensions — e.g. pyyaml, msgpack (including statically   linked C libraries such as libyaml)</li> <li>Rust extensions — e.g. orjson, rpds-py</li> <li>Stable ABI (abi3) extensions — e.g. cryptography, bcrypt</li> </ul>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#validation-against-conda-forge","level":2,"title":"Validation against conda-forge","text":"<p>The whl2conda test suite includes an external comparison suite that converts a curated sample of representative binary PyPI packages (C extensions, Cython, stable-ABI and non-abi3 rust extensions, and packages with bundled libraries) and semantically compares each result against the real conda-forge package of the same version using <code>whl2conda diff</code>. It runs monthly in CI and developers can run it with:</p> <pre><code>pixi run compare-conda-forge\n</code></pre> <p>This requires network access, downloads packages from PyPI and anaconda.org (cached across runs), and writes a summary report to <code>compare-report.md</code> / <code>compare-report.json</code>.</p> <p>To vet your own conversion, compare it against a reference package built from a recipe (if one exists) using <code>whl2conda diff</code>, and install and test it with <code>whl2conda install</code>.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#known-limitations","level":2,"title":"Known limitations","text":"<p>The converter detects and refuses the known-bad cases it can identify: wheels that are not pure python (without <code>--allow-impure</code>), unsupported wheel platform tags, and local version suffixes (below). It warns about wheels that bundle shared libraries. The remaining limitations listed here are inherent to wheel conversion and are the user's responsibility to evaluate.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#local-version-suffixes","level":3,"title":"Local version suffixes","text":"<p>Wheels with local version suffixes (e.g. <code>torch-2.1.0+cu121</code>) indicate custom build variants such as CUDA-specific builds. These are blocked with an error because they bundle variant-specific libraries that require careful dependency management not possible through simple wheel conversion.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#bundled-shared-libraries","level":3,"title":"Bundled shared libraries","text":"<p>Binary wheels repaired by tools like auditwheel, delocate, or delvewheel bundle copies of the shared libraries they link against (in <code>&lt;package&gt;.libs/</code> or <code>.dylibs/</code> directories). The converter warns when it detects such vendored libraries. The converted package includes these bundled copies as-is — unlike an equivalent conda-forge package, which would declare shared library dependencies instead — which can lead to:</p> <ul> <li>Version conflicts with conda-installed libraries</li> <li>Missing transitive dependencies not declared in the wheel metadata</li> <li>ABI incompatibilities when mixed with conda-forge packages</li> </ul> <p>This usually works (the same copies are used from PyPI installs), but duplicates libraries in the environment and bypasses conda's dependency management for them.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#no-run_exports-or-build-metadata","level":3,"title":"No <code>run_exports</code> or build metadata","text":"<p>Unlike conda-forge packages, converted wheels lack <code>run_exports</code> and detailed build metadata. Downstream packages that depend on the converted package will not automatically inherit correct library dependencies. You may need to manually specify additional dependencies using the <code>-A</code>/<code>--add-dependency</code> flag during conversion. Not detected by the converter.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#dependencies-must-exist-on-the-target-channel","level":3,"title":"Dependencies must exist on the target channel","text":"<p>Dependency names are renamed using the standard pypi-to-conda rename table (plus any project-specific renames), but the converter does not verify that the resulting conda packages actually exist on the channel you will install from. A missing or incorrectly named dependency only surfaces when the package is installed. Not detected by the converter — test with <code>whl2conda install</code>.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#multi-platform-fat-macos-wheels","level":3,"title":"Multi-platform (fat) macOS wheels","text":"<p>macOS wheels may support several platforms at once: <code>universal2</code> wheels contain both x86_64 and arm64 code, and some wheels are tagged for <code>x86_64</code>, <code>arm64</code>, and <code>universal2</code> simultaneously. Conversion produces a single conda package for one platform: the platform matching the current system is preferred; otherwise a <code>universal2</code> wheel is converted for <code>osx-arm64</code>. Use the <code>--platform-tag</code> option to select the target platform explicitly:</p> <pre><code>whl2conda convert orjson-3.11.9-*.whl --allow-impure \\\n    --platform-tag macosx_10_15_x86_64\n</code></pre> <p>Alternatively, <code>--all-platforms</code> generates one package per supported platform in a single conversion, writing each package into a <code>&lt;subdir&gt;/</code> subdirectory of the output directory (packages for different platforms cannot share a directory, since conda package file names do not include the platform):</p> <pre><code>whl2conda convert orjson-3.11.9-*.whl --allow-impure --all-platforms\n# -&gt; out-dir/osx-64/orjson-3.11.9-py313_0.conda\n#    out-dir/osx-arm64/orjson-3.11.9-py313_0.conda\n</code></pre>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/binary-conversion.html#no-pre-compiled-pyc-files","level":3,"title":"No pre-compiled <code>.pyc</code> files","text":"<p>Conda-forge packages typically include pre-compiled Python bytecode (<code>.pyc</code> files) for faster first imports. Converted wheels do not include these because wheels themselves omit <code>.pyc</code> files per PEP 427, and cross-platform conversions cannot generate valid bytecode for a different Python version than the one running the conversion. Conda will compile <code>.pyc</code> files at install time, so this only affects first-import performance.</p>","path":["User Guide","Binary Wheel Conversion"],"tags":[]},{"location":"guide/limitations.html","level":1,"title":"Limitations","text":"<p>whl2conda supports the conversion of generic pure python wheels into noarch python conda packages.</p> <p>It has the following limitations and known issues, some of which  will be addressed in future releases.</p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/limitations.html#arbitrary-equality-clause-in-version-specifiers-has-no-conda-equivalent","level":2,"title":"Arbitrary equality clause in version specifiers has no conda equivalent","text":"<p>The arbitrary equality clause <code>===</code> is not supported by conda and there is no equivalent. This clause is also heavily discouraged  in dependencies and probably will not occur that often in practice.</p> <p>We handle this by simply changing <code>===</code> to <code>==</code> but since this will often not work we also issue a warning.</p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/limitations.html#file-permissions-are-not-copied-when-run-on-windows","level":2,"title":"File permissions are not copied when run on Windows","text":"<p>Executable file permissions are copied from the wheel when conversion is run on MacOS or Linux but not Windows. When converting packages that contain scripts with execute permissions (intended for use on Linux/MacOS), make sure to avoid Windows when doing the conversion (see issue 135)</p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/limitations.html#cannot-convert-from-sdist","level":2,"title":"Cannot convert from sdist","text":"<p>Conversion from python sdist distributions is not currently supported.  This could possibly be supported in the future (see issue 78).</p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/limitations.html#cannot-convert-from-eggs","level":2,"title":"Cannot convert from eggs","text":"<p>Python egg files  are also not supported. Since this file format is deprecated and  uploads to pypi are no longer allowed, we have no plans to support this format.</p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/limitations.html#cannot-handle-dependencies-using-extras","level":2,"title":"Cannot handle dependencies using extras","text":"<p>Currently, for any dependencies that declare extras, the extras dependencies are not included. For instance, the dependency:</p> <pre><code>black[jupyter]\n</code></pre> <p>will include the <code>black</code> dependency itself, but not any extra dependencies that are declared with the <code>extra == 'jupyter'</code> marker. This will be addressed in a future release. See issue 36.</p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/limitations.html#only-supports-noarch-python-by-default","level":2,"title":"Only supports noarch python by default","text":"<p>By default, only generic python conda packages with <code>noarch: python</code> will be generated.</p> <p>Binary wheels can be converted using the <code>--allow-impure</code> flag. See the Binary Conversion guide for details and limitations.</p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/limitations.html#dependencies-with-environment-markers","level":2,"title":"Dependencies with environment markers","text":"<p>For noarch conversions, dependencies with environment markers are not included in the conda package. For binary conversions, markers are evaluated against the target platform and matching dependencies are included. See the Binary Conversion guide for details. </p>","path":["User Guide","Limitations"],"tags":[]},{"location":"guide/pyproject.html","level":1,"title":"Pyproject","text":"<p>If you are working with a project using a <code>pyproject.toml</code> file, you can add configuration options for whl2conda in the <code>[tool.whl2conda]</code> section of the file rather than using command line options.</p> <p>The easiest way to start with these options, is to generate default options with comments using:</p> <pre><code>$ whl2conda config --generate-pyproject my-project-dir/\n</code></pre> <p>This is safe to use on an existing pyproject file and will not  overwrite any existing options.</p> <p>In general, whl2conda command line options will override options specified in the <code>pyproject.toml</code> file.</p> <p>The available options are described below.</p>","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/pyproject.html#pyprojecttoml-options","level":2,"title":"pyproject.toml options","text":"","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/pyproject.html#conda-name","level":3,"title":"conda-name","text":"<p>Overrides name of conda package generated by whl2conda. If empty, default is taken from <code>project.name</code> (or <code>tool.poetry.name</code> if build backend is poetry).</p> <p>This is overridden by the <code>--name</code> convert option.</p>","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/pyproject.html#wheel-dir","level":3,"title":"wheel-dir","text":"<p>The default directory for whl2conda to look for wheels. Specified relative to the project root. If not specified, defaults to 'dist'.</p> <p>Overridden by the <code>--wheel-dir</code> convert option.</p>","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/pyproject.html#out-dir","level":3,"title":"out-dir","text":"<p>The default directory for writing conda packages. If not specified, this will be the wheel directory.</p> <p>Overridden by the <code>--out-dir</code> convert option.</p>","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/pyproject.html#conda-format","level":3,"title":"conda-format","text":"<p>The default output conda package format. Valid values are:</p> <ul> <li><code>\"V1\"</code> / <code>\".tar.bz2\"</code>: original .tar.bz2 package format</li> <li><code>\"V2\"</code> / <code>\".conda\"</code>: new V2 .conda package format</li> <li><code>\"TREE\"</code>:  output package as directory tree (for debugging)</li> </ul> <p>Overridden by the <code>--format</code> convert option.</p>","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/pyproject.html#dependency-rename","level":3,"title":"dependency-rename","text":"<p>An optional list of pypi/conda rename patterns. Each entry should be a two element list of the form:</p> <pre><code>   [\"&lt;pypi-name-pattern&gt;\", \"&lt;conda-name&gt;\"]',\n</code></pre> <p>Where <code>&lt;pypi-name-pattern&gt;</code> may either just be the pypi package name or is a Python regular expression that will match the full name, and where <code>&lt;conda-name&gt;</code> can contain substitution expressions. For example, to rename all <code>acme-&lt;foo&gt;</code> packages to <code>acme.&lt;foo&gt;</code>:</p> <pre><code>   [\"acme-(.*)\", \"acme.$1\"]\n</code></pre>","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/pyproject.html#extra-dependencies","level":3,"title":"extra-dependencies","text":"<p>An optional list of extra conda dependencies.</p>","path":["User Guide","Pyproject"],"tags":[]},{"location":"guide/renaming.html","level":1,"title":"Renaming","text":"","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#overview","level":2,"title":"Overview","text":"<p>Python packages found on pypi.org and installed using pip often have the same name as packages found on anaconda.org and installed using conda. For instance, you can install <code>pylint</code> using:</p> pipconda <pre><code>$ pip install pylint\n</code></pre> <pre><code>$ conda install -c conda-forge pylint\n</code></pre> <p>However, pip and conda use totally separate packaging systems and there is no inherent requirement that package names match between the two, and  indeed there are quite a few exceptions, for example:</p> pipconda <pre><code>$ pip install numpy-quaternion \n$ pip install tables\n$ pip install torch\n</code></pre> <pre><code>$ conda install -c conda-forge quaternion \n$ conda install -c conda-forge pytables\n$ conda install -c conda-forge pytorch\n</code></pre> <p>So when converting from wheels to conda packages, it is important that these differences be handled by the tool.</p>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#standard-renames","level":2,"title":"Standard renames","text":"<p>The whl2conda tool maintains a table of automatic renaming rules that is taken from mappings collected automatically from tools supporting the public conda-forge repository. whl2conda includes a static copy of this table collated when the whl2conda package was built, but also  supports the ability to maintain and update a locally cached copy dynamically. If there is a package new to conda-forge that may have appeared since installing whl2conda, you can update your local cache using:</p> <pre><code>$ whl2conda config --update-std-renames\n</code></pre> <p>You can also configure whl2conda to automatically update the standard rename mappings prior to operations that require them via a persistent user setting:</p> <pre><code>$ whl2conda config --set auto-update-std-renames true\n</code></pre> <p>The cache file is kept in a location in your user directory that is specific  to your operating system:</p> <ul> <li>Linux: <code>~/.cache/whl2conda/stdrename.json</code></li> <li>MacOS: <code>~/Library/Caches/whl2conda/stdrename.json</code></li> <li>Windows: <code>~\\AppData\\Local\\whl2conda\\Cache\\stdrename.json</code></li> </ul> <p>This file is simply a JSON dictionary mapping pypi names to conda names.</p> <p>If you want to generate a copy of this file in another location (e.g. for use by other development tools), you can add a path to the <code>--update-std-renames</code> option:</p> <pre><code>$ whl2conda config --update-std-renames pypi-conda-renames.json\n</code></pre>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#manual-rules","level":2,"title":"Manual rules","text":"<p>The implicit standard renaming support takes care of most renaming issues for publicly available packages, but may not work for packages that come from alternative channels, which may be private to your organization.</p> <p>If you encounter these, you can add command line options to <code>whl2conda convert</code> to rename packages and also to add or drop packages. You may also rename the package you are building.</p>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#adding-extra-packages","level":3,"title":"Adding extra packages","text":"<p>You can add one or more extra package dependencies using the <code>-A</code> / <code>--add-dependency</code> option. This can be a conda package name and version spec, e.g.:</p> <pre><code>$ whl2conda convert -A 'extra-package &gt;=1.2.3' ...\n</code></pre> <p>You can use this to add dependencies for conda packages that perhaps do not exist on pypi.</p>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#dropping-a-package","level":3,"title":"Dropping a package","text":"<p>Likewise, you can drop packages using <code>-D</code> / <code>--drop-dependency</code> with just the package name:</p> <pre><code>$ whl2conda convert -D 'some-pypi-only-package'\n</code></pre> <p>This option also allows you to use python regular expressions to drop any package that matches a pattern:</p> <pre><code>$ whl2conda convert -D 'acme-.*'\n</code></pre>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#renaming-dependencies","level":3,"title":"Renaming dependencies","text":"<p>To rename dependencies, use <code>-R</code> / <code>--dependency-rename</code> with two arguments, the pypi name followed by the conda name.  You can also use regular expressions with capture groups, where <code>$&lt;n&gt;</code> will be replaced with the nth capture and <code>${name}</code> will be replaced with the named capture group with given name.</p> plainregular expressionnamed regular expression <pre><code>$ whl2conda convert -R acme-widgets acme.widgets\n</code></pre> <pre><code>$ whl2conda convert -R 'acme-(.*)' 'acme.$1'\n</code></pre> <pre><code>$ whl2conda convert -R 'acme-(?P&lt;part&gt;.*)' 'acme.${part}'\n</code></pre>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#renaming-converted-package","level":3,"title":"Renaming converted package","text":"<p>By default, the name of the generated conda package will be taken from the project name in the <code>pyproject.toml</code> if there is one, otherwise from the name in the wheel. This can be overridden using the <code>--name</code> command line option:</p> <pre><code>$ whl2conda convert acme-widgets-1.2.3-py3-none-any.whl --name acme-pywidgets\n</code></pre>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#specifying-rules-in-pyprojecttoml-file","level":2,"title":"Specifying rules in pyproject.toml file","text":"<p>If you are using a <code>pyproject.toml</code> file for your project, you can instead specify how dependencies are modified in the tool options. This is described in the next section.</p>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/renaming.html#hiding-pip-dependencies-in-dist-info-directory-for-conda","level":2,"title":"Hiding pip dependencies in dist-info directory for conda","text":"<p>The standard method of building python conda package involves running <code>pip install</code> or the equivalent, which results in a the package's original pip dependencies and other metadata being saved to the <code>METADATA</code> file in the package's <code>.dist-info</code> directory in <code>site-packages/</code> for the environment in which it is installed. However, in a conda environment, these dependencies may seem to be incompatible with what is actually installed by conda. This can sometimes cause serious problems if pip or other standard  python packaging tools attempt to check or update these dependencies.</p> <p>Since the actual dependencies for packages installed by conda are described by the conda package's metadata, and not the metadata saved in the <code>.dist-info</code>, whl2conda by default will turn all regular dependencies in the dist-info into extra dependencies using  the name <code>original</code>. So if you look at the <code>METADATA</code> file in the  dist-info of the generated conda package, you will see entries like:</p> <pre><code>Requires-Dist: some-package &gt;=1.2; extra == 'original'\n</code></pre> <p>If you want to leave these dependencies unchanged you can use the <code>--keep-pip-dependencies</code> option to <code>whl2conda convert</code>. This is  not recommended unless you know that you need them.</p>","path":["User Guide","Renaming"],"tags":[]},{"location":"guide/testing.html","level":1,"title":"Testing","text":"","path":["User Guide","Testing"],"tags":[]},{"location":"guide/testing.html#installing-into-a-test-environment","level":2,"title":"Installing into a test environment","text":"<p>You will probably want to test your generated conda packages before deploying them. Currently, <code>conda install</code> only supports installing conda package files without their dependencies, so <code>whl2conda</code> provides an <code>install</code> subcommand to install one or more package files into a test environment along with their dependencies:</p> <pre><code>$ whl2conda install mypackage-1.2.3-py_0.conda -n test-env\n</code></pre> <p>If you want to create an environment in a temporary directory:</p> <pre><code>$ whl2conda install mypackage-1.2.3-py_0.conda --create -p tmp-dir\n</code></pre> <p>You can also add additional dependencies or other options to pass to the underlying <code>conda install</code> command:</p> <pre><code>$ whl2conda install mypackage-1.2.3-py_0.conda \\\n   --create -p tmp-dir \\\n   --extra pytest -c my-channel\n</code></pre> <p>If you are building multiple packages with an interdependency you should install them in a single install command, e.g.:</p> <pre><code>$ whl2conda install mypackage-1.2.3-py_0.conda mycorepackage-1.2.3-py_0.conda ...\n</code></pre>","path":["User Guide","Testing"],"tags":[]},{"location":"guide/testing.html#installing-into-conda-bld","level":2,"title":"Installing into conda-bld","text":"<p>Once you are done testing, you may upload your package to a conda channel using the appropriate tool (e.g. <code>anaconda upload</code> or <code>curl</code>), or you may install into your local conda-bld directory to support <code>conda install --use-local</code>. You can do this using:</p> <pre><code>$ whl2conda install mypackage-1.2.3-py_0.conda --conda-bld\n</code></pre>","path":["User Guide","Testing"],"tags":[]},{"location":"guide/testing.html#comparing-packages","level":2,"title":"Comparing packages","text":"<p>You may wish to compare generated packages against those generated by conda-build or rattler-build (e.g. from conda-forge) in order both to understand what this tool is doing and to verify that nothing important is missing. You can do this with the <code>whl2conda diff</code> command:</p> <pre><code>$ whl2conda diff \\\n   dist/mypackage-1.2.3-py_0.conda \\\n   ~/miniforge3/conda-bld/noarch/mypackage-1.2.3-py_0.tar.bz2\n</code></pre> <p>By default, this semantically analyzes the differences between the two packages and prints a report of notable and unexpected differences — missing dependencies, missing or altered files, unrenamed pip dependencies, mismatched entry points, and so on — while suppressing differences that are expected when comparing a whl2conda-generated package against a recipe-built one, such as differing build strings and timestamps, run-export dependencies added by compilers, regenerated entry point scripts, differing binary module contents, and dist-info bookkeeping details. The command exits with a non-zero status if any unexpected differences are found, so it can be used in scripts and CI.</p> <p>Useful options (see the command reference for the full list):</p> <ul> <li><code>--all</code> also shows the expected differences</li> <li><code>--strict</code> treats notable differences as errors</li> <li><code>--ignore &lt;category&gt;</code> suppresses a category of differences</li> <li><code>--run-export &lt;name&gt;</code> treats additional dependency names as benign   run-exports when only present in the reference package</li> <li><code>--json</code> emits the analysis as JSON</li> </ul> <p>Alternatively, you can inspect the raw differences with your favorite directory diff tool using the <code>-T</code> option. This will unpack the packages into temporary directories, normalize the metadata files to minimize mismatches and run the specified diff tool with the given arguments:</p> <pre><code>$ whl2conda diff \\\n   dist/mypackage-1.2.3-py_0.conda \\\n   ~/miniforge3/conda-bld/noarch/mypackage-1.2.3-py_0.tar.bz2 \\\n   -T kdiff3\n</code></pre>","path":["User Guide","Testing"],"tags":[]},{"location":"reference/links.html","level":1,"title":"Links","text":"","path":["Reference","Links"],"tags":[]},{"location":"reference/links.html#conda","level":2,"title":"Conda","text":"<ul> <li>conda file format</li> <li>package spec</li> <li>CEP-20 - support for abi3 python packages</li> <li>conda-package-handling tool (cph)</li> <li>conda-build</li> </ul>","path":["Reference","Links"],"tags":[]},{"location":"reference/links.html#wheels","level":2,"title":"Wheels:","text":"<ul> <li>Binary distribution format</li> <li>Recording installed packages</li> <li>Current meta-data specification</li> <li>Platform compatibility tags</li> <li>Version specifiers</li> <li>Name normalization</li> <li>Dependency specifiers (PEP 508)</li> <li>Wheel 1.0 file format (PEP 427)</li> <li>Wheel metadata 1.0 (PEP 241)</li> <li>Wheel metadata 1.1 (PEP 314)</li> <li>Wheel metadata 1.2 (PEP 345)</li> <li>Wheel metadata 2.1 (PEP 566)</li> <li>Wheel metadata 2.2 (PEP 643)</li> <li>Wheel metadata 2.3 (PEP 685)</li> <li>Wheel metadata 2.4 (PEP 639) - license expression and license files</li> <li>Wheel metadata 2.5 (PEP 770) - SBOM documents</li> </ul>","path":["Reference","Links"],"tags":[]},{"location":"reference/links.html#pyprojecttoml-metadata-format","level":2,"title":"pyproject.toml metadata format:","text":"<ul> <li>Current specification</li> <li>PEP 621</li> <li>PEP 639 - license files</li> <li>PEP 725 - external dependencies</li> </ul>","path":["Reference","Links"],"tags":[]},{"location":"reference/links.html#repository-layout","level":2,"title":"Repository layout","text":"<ul> <li>PEP 503 - simple repository API and normalized package names</li> </ul>","path":["Reference","Links"],"tags":[]},{"location":"reference/links.html#poetry-issues","level":2,"title":"Poetry issues:","text":"<ul> <li>Poetry PEP 621 support</li> </ul>","path":["Reference","Links"],"tags":[]},{"location":"reference/api/compare.html","level":1,"title":"whl2conda.api.compare","text":"<p>Semantic comparison of conda packages.</p> <p>This module compares two conda packages - typically one generated by whl2conda from a PyPI wheel and a reference package built from a recipe (e.g. by conda-build or rattler-build for conda-forge) - and reports their differences, classifying each as expected (benign differences inherent to the different build processes), notable, or unexpected errors.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare-classes","level":2,"title":"Classes","text":"","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.CompareOptions","level":3,"title":"CompareOptions  <small><code>dataclass</code></small>","text":"<p>Options controlling conda package comparison.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>@dataclass(slots=True)\nclass CompareOptions:\n    \"\"\"Options controlling conda package comparison.\"\"\"\n\n    strict: bool = False\n    \"\"\"Promote NOTICE differences to ERROR.\"\"\"\n\n    ignore: set[DiffCategory] = field(default_factory=set)\n    \"\"\"Categories demoted to EXPECTED severity.\"\"\"\n\n    extra_run_exports: set[str] = field(default_factory=set)\n    \"\"\"Additional dependency names treated as benign run-exports.\"\"\"\n\n    renames: dict[str, str] | None = None\n    \"\"\"pypi to conda package rename map.\n\n    Defaults to the standard rename table from [load_std_renames][(m)..stdrename.].\n    \"\"\"\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.CompareOptions-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.CompareOptions.extra_run_exports","level":5,"title":"extra_run_exports  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>extra_run_exports: set[str] = field(default_factory=set)\n</code></pre> <p>Additional dependency names treated as benign run-exports.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.CompareOptions.ignore","level":5,"title":"ignore  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>ignore: set[DiffCategory] = field(default_factory=set)\n</code></pre> <p>Categories demoted to EXPECTED severity.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.CompareOptions.renames","level":5,"title":"renames  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>renames: dict[str, str] | None = None\n</code></pre> <p>pypi to conda package rename map.</p> <p>Defaults to the standard rename table from load_std_renames.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.CompareOptions.strict","level":5,"title":"strict  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>strict: bool = False\n</code></pre> <p>Promote NOTICE differences to ERROR.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult","level":3,"title":"ComparisonResult  <small><code>dataclass</code></small>","text":"<p>Result of comparing two conda packages.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>@dataclass(slots=True)\nclass ComparisonResult:\n    \"\"\"Result of comparing two conda packages.\"\"\"\n\n    package1: Path\n    \"\"\"First package (the package under test).\"\"\"\n\n    package2: Path\n    \"\"\"Second package (the reference package).\"\"\"\n\n    differences: list[Difference]\n    \"\"\"All differences found, including expected ones.\"\"\"\n\n    @property\n    def errors(self) -&gt; list[Difference]:\n        \"\"\"Differences with ERROR severity.\"\"\"\n        return [d for d in self.differences if d.severity &gt;= Severity.ERROR]\n\n    @property\n    def ok(self) -&gt; bool:\n        \"\"\"True if no ERROR severity differences were found.\"\"\"\n        return not self.errors\n\n    def report(self, *, min_severity: Severity = Severity.NOTICE) -&gt; str:\n        \"\"\"Generate a human readable report.\n\n        Args:\n            min_severity: only include differences of at least this severity.\n\n        Returns:\n            Multi-line report string.\n        \"\"\"\n        lines = [f\"Comparing {self.package1}\", f\"   against {self.package2}\", \"\"]\n        shown = [d for d in self.differences if d.severity &gt;= min_severity]\n        suppressed = len(self.differences) - len(shown)\n        for d in sorted(shown, key=lambda d: (-d.severity, d.category.value, d.key)):\n            lines.append(str(d))\n            if d.left is not None:\n                lines.append(f\"    package1: {d.left}\")\n            if d.right is not None:\n                lines.append(f\"    package2: {d.right}\")\n        n_errors = len(self.errors)\n        n_notices = sum(1 for d in self.differences if d.severity == Severity.NOTICE)\n        lines.append(\"\")\n        summary = f\"{n_errors} errors, {n_notices} notices\"\n        if suppressed:\n            summary += f\" ({suppressed} differences suppressed)\"\n        lines.append(summary)\n        return \"\\n\".join(lines)\n\n    def to_json(self) -&gt; dict[str, Any]:\n        \"\"\"JSON-compatible dictionary representation.\"\"\"\n\n        def _safe(val: Any) -&gt; Any:\n            if val is None or isinstance(val, (bool, int, float, str)):\n                return val\n            if isinstance(val, (list, tuple, set, frozenset)):\n                return sorted(str(v) for v in val)\n            return str(val)\n\n        return {\n            \"package1\": str(self.package1),\n            \"package2\": str(self.package2),\n            \"ok\": self.ok,\n            \"differences\": [\n                {\n                    \"category\": d.category.value,\n                    \"severity\": d.severity.name,\n                    \"key\": d.key,\n                    \"description\": d.description,\n                    \"left\": _safe(d.left),\n                    \"right\": _safe(d.right),\n                }\n                for d in self.differences\n            ],\n        }\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult.differences","level":5,"title":"differences  <small><code>instance-attribute</code></small>","text":"<pre><code>differences: list[Difference]\n</code></pre> <p>All differences found, including expected ones.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult.errors","level":5,"title":"errors  <small><code>property</code></small>","text":"<pre><code>errors: list[Difference]\n</code></pre> <p>Differences with ERROR severity.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult.ok","level":5,"title":"ok  <small><code>property</code></small>","text":"<pre><code>ok: bool\n</code></pre> <p>True if no ERROR severity differences were found.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult.package1","level":5,"title":"package1  <small><code>instance-attribute</code></small>","text":"<pre><code>package1: Path\n</code></pre> <p>First package (the package under test).</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult.package2","level":5,"title":"package2  <small><code>instance-attribute</code></small>","text":"<pre><code>package2: Path\n</code></pre> <p>Second package (the reference package).</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult-functions","level":4,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult.report","level":5,"title":"report","text":"<pre><code>report(*, min_severity: Severity = Severity.NOTICE) -&gt; str\n</code></pre> <p>Generate a human readable report.</p> PARAMETER DESCRIPTION <code>min_severity</code> <p>only include differences of at least this severity.</p> <p> TYPE: <code>Severity</code> DEFAULT: <code>NOTICE</code> </p> RETURNS DESCRIPTION <code>str</code> <p>Multi-line report string.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>def report(self, *, min_severity: Severity = Severity.NOTICE) -&gt; str:\n    \"\"\"Generate a human readable report.\n\n    Args:\n        min_severity: only include differences of at least this severity.\n\n    Returns:\n        Multi-line report string.\n    \"\"\"\n    lines = [f\"Comparing {self.package1}\", f\"   against {self.package2}\", \"\"]\n    shown = [d for d in self.differences if d.severity &gt;= min_severity]\n    suppressed = len(self.differences) - len(shown)\n    for d in sorted(shown, key=lambda d: (-d.severity, d.category.value, d.key)):\n        lines.append(str(d))\n        if d.left is not None:\n            lines.append(f\"    package1: {d.left}\")\n        if d.right is not None:\n            lines.append(f\"    package2: {d.right}\")\n    n_errors = len(self.errors)\n    n_notices = sum(1 for d in self.differences if d.severity == Severity.NOTICE)\n    lines.append(\"\")\n    summary = f\"{n_errors} errors, {n_notices} notices\"\n    if suppressed:\n        summary += f\" ({suppressed} differences suppressed)\"\n    lines.append(summary)\n    return \"\\n\".join(lines)\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.ComparisonResult.to_json","level":5,"title":"to_json","text":"<pre><code>to_json() -&gt; dict[str, Any]\n</code></pre> <p>JSON-compatible dictionary representation.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>def to_json(self) -&gt; dict[str, Any]:\n    \"\"\"JSON-compatible dictionary representation.\"\"\"\n\n    def _safe(val: Any) -&gt; Any:\n        if val is None or isinstance(val, (bool, int, float, str)):\n            return val\n        if isinstance(val, (list, tuple, set, frozenset)):\n            return sorted(str(v) for v in val)\n        return str(val)\n\n    return {\n        \"package1\": str(self.package1),\n        \"package2\": str(self.package2),\n        \"ok\": self.ok,\n        \"differences\": [\n            {\n                \"category\": d.category.value,\n                \"severity\": d.severity.name,\n                \"key\": d.key,\n                \"description\": d.description,\n                \"left\": _safe(d.left),\n                \"right\": _safe(d.right),\n            }\n            for d in self.differences\n        ],\n    }\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.DiffCategory","level":3,"title":"DiffCategory","text":"<p>               Bases: <code>Enum</code></p> <p>Category of a difference between two conda packages.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>class DiffCategory(enum.Enum):\n    \"\"\"Category of a difference between two conda packages.\"\"\"\n\n    # index.json / metadata\n    PACKAGE_NAME = \"package-name\"\n    PACKAGE_VERSION = \"package-version\"\n    BUILD_STRING = \"build-string\"\n    BUILD_NUMBER = \"build-number\"\n    NOARCH_VS_PLATFORM = \"noarch-vs-platform\"\n    LICENSE = \"license\"\n    ABOUT_FIELD = \"about-field\"\n    TIMESTAMP = \"timestamp\"\n    # dependencies\n    DEP_MISSING = \"dep-missing\"\n    DEP_EXTRA = \"dep-extra\"\n    DEP_RUN_EXPORT = \"dep-run-export\"\n    DEP_VERSION = \"dep-version\"\n    DEP_UNRENAMED = \"dep-unrenamed\"\n    PYTHON_PIN = \"python-pin\"\n    # files\n    FILE_MISSING = \"file-missing\"\n    FILE_EXTRA = \"file-extra\"\n    FILE_CONTENT = \"file-content\"\n    BINARY_CONTENT = \"binary-content\"\n    PYCACHE = \"pycache\"\n    ENTRY_POINT = \"entry-point\"\n    # dist-info\n    DIST_INFO_METADATA = \"dist-info-metadata\"\n    DIST_INFO_OTHER = \"dist-info-other\"\n    # info/ bookkeeping\n    INFO_EXTRA_FILE = \"info-extra-file\"\n\n    def __str__(self) -&gt; str:\n        return self.value\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference","level":3,"title":"Difference  <small><code>dataclass</code></small>","text":"<p>A single difference between two conda packages.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>@dataclass(frozen=True, slots=True)\nclass Difference:\n    \"\"\"A single difference between two conda packages.\"\"\"\n\n    category: DiffCategory\n    \"\"\"Category of the difference.\"\"\"\n\n    severity: Severity\n    \"\"\"Severity of the difference.\"\"\"\n\n    key: str\n    \"\"\"Locus of the difference, e.g. `index.json:depends` or a file path.\"\"\"\n\n    description: str\n    \"\"\"Human readable one line description.\"\"\"\n\n    left: Any = None\n    \"\"\"Relevant value from the first package, if any.\"\"\"\n\n    right: Any = None\n    \"\"\"Relevant value from the second (reference) package, if any.\"\"\"\n\n    def __str__(self) -&gt; str:\n        return f\"{self.severity.name} {self.category} {self.key}: {self.description}\"\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference.category","level":5,"title":"category  <small><code>instance-attribute</code></small>","text":"<pre><code>category: DiffCategory\n</code></pre> <p>Category of the difference.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference.description","level":5,"title":"description  <small><code>instance-attribute</code></small>","text":"<pre><code>description: str\n</code></pre> <p>Human readable one line description.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference.key","level":5,"title":"key  <small><code>instance-attribute</code></small>","text":"<pre><code>key: str\n</code></pre> <p>Locus of the difference, e.g. <code>index.json:depends</code> or a file path.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference.left","level":5,"title":"left  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>left: Any = None\n</code></pre> <p>Relevant value from the first package, if any.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference.right","level":5,"title":"right  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>right: Any = None\n</code></pre> <p>Relevant value from the second (reference) package, if any.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Difference.severity","level":5,"title":"severity  <small><code>instance-attribute</code></small>","text":"<pre><code>severity: Severity\n</code></pre> <p>Severity of the difference.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Severity","level":3,"title":"Severity","text":"<p>               Bases: <code>IntEnum</code></p> <p>Severity of a difference between two conda packages.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>class Severity(enum.IntEnum):\n    \"\"\"Severity of a difference between two conda packages.\"\"\"\n\n    EXPECTED = 0\n    \"\"\"Benign difference inherent to the different build processes.\"\"\"\n\n    NOTICE = 1\n    \"\"\"Notable difference that is not necessarily a problem.\"\"\"\n\n    ERROR = 2\n    \"\"\"Unexpected difference that likely indicates a problem.\"\"\"\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Severity-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Severity.ERROR","level":5,"title":"ERROR  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>ERROR = 2\n</code></pre> <p>Unexpected difference that likely indicates a problem.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Severity.EXPECTED","level":5,"title":"EXPECTED  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>EXPECTED = 0\n</code></pre> <p>Benign difference inherent to the different build processes.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.Severity.NOTICE","level":5,"title":"NOTICE  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>NOTICE = 1\n</code></pre> <p>Notable difference that is not necessarily a problem.</p>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare-functions","level":2,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/compare.html#whl2conda.api.compare.compare_conda_packages","level":3,"title":"compare_conda_packages","text":"<pre><code>compare_conda_packages(\n    package1: Path,\n    package2: Path,\n    *,\n    options: CompareOptions | None = None\n) -&gt; ComparisonResult\n</code></pre> <p>Semantically compare two conda packages.</p> <p>The comparison is directional: <code>package1</code> is the package under test (e.g. generated by whl2conda) and <code>package2</code> is the reference package (e.g. from conda-forge). Differences that are inherent to the different build processes are reported with EXPECTED severity; differences that likely indicate a conversion problem are reported as errors.</p> PARAMETER DESCRIPTION <code>package1</code> <p><code>.conda</code>/<code>.tar.bz2</code> file or extracted package directory for the package under test.</p> <p> TYPE: <code>Path</code> </p> <code>package2</code> <p><code>.conda</code>/<code>.tar.bz2</code> file or extracted package directory for the reference package.</p> <p> TYPE: <code>Path</code> </p> <code>options</code> <p>comparison options.</p> <p> TYPE: <code>CompareOptions | None</code> DEFAULT: <code>None</code> </p> RETURNS DESCRIPTION <code>ComparisonResult</code> <p>Comparison result with the list of differences found.</p> Source code in <code>src/whl2conda/api/compare.py</code> <pre><code>def compare_conda_packages(\n    package1: Path,\n    package2: Path,\n    *,\n    options: CompareOptions | None = None,\n) -&gt; ComparisonResult:\n    \"\"\"Semantically compare two conda packages.\n\n    The comparison is directional: `package1` is the package under\n    test (e.g. generated by whl2conda) and `package2` is the reference\n    package (e.g. from conda-forge). Differences that are inherent to\n    the different build processes are reported with EXPECTED severity;\n    differences that likely indicate a conversion problem are reported\n    as errors.\n\n    Args:\n        package1: `.conda`/`.tar.bz2` file or extracted package directory\n            for the package under test.\n        package2: `.conda`/`.tar.bz2` file or extracted package directory\n            for the reference package.\n        options: comparison options.\n\n    Returns:\n        Comparison result with the list of differences found.\n    \"\"\"\n    options = options or CompareOptions()\n    with tempfile.TemporaryDirectory(prefix=\"whl2conda-compare-\") as tmpdir:\n        tmp_path = Path(tmpdir)\n        pkg1 = _ExtractedPackage(_extract_if_needed(package1, tmp_path / \"pkg1\"))\n        pkg2 = _ExtractedPackage(_extract_if_needed(package2, tmp_path / \"pkg2\"))\n        comparer = _PackageComparer(pkg1, pkg2, options)\n        differences = comparer.compare()\n    return ComparisonResult(package1, package2, differences)\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.compare"],"tags":[]},{"location":"reference/api/converter.html","level":1,"title":"whl2conda.api.converter","text":"<p>Converter API</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter-attributes","level":2,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.pip_version_re","level":3,"title":"pip_version_re  <small><code>module-attribute</code></small>","text":"<pre><code>pip_version_re = compile(\n    \"^\\\\s*(?P&lt;operator&gt;[=&lt;&gt;!~]+)\\\\s*(?P&lt;version&gt;\"\n    + PIP_VERSION_PATTERN\n    + \")\\\\s*$\",\n    VERBOSE | IGNORECASE,\n)\n</code></pre> <p>Regular expression matching pip version spec</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter-classes","level":2,"title":"Classes","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaPackageFormat","level":3,"title":"CondaPackageFormat","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Supported output package formats</p> <ul> <li>V1: original conda format as .tar.bz2 file</li> <li>V2: newer .conda format</li> <li>TREE: dumps package out as a directory tree (for debugging)</li> </ul> Source code in <code>src/whl2conda/impl/pyproject.py</code> <pre><code>class CondaPackageFormat(str, enum.Enum):\n    \"\"\"\n    Supported output package formats\n\n    * V1: original conda format as .tar.bz2 file\n    * V2: newer .conda format\n    * TREE: dumps package out as a directory tree (for debugging)\n    \"\"\"\n\n    V1 = \".tar.bz2\"\n    V2 = \".conda\"\n    TREE = \".tree\"\n\n    @classmethod\n    def from_string(cls, name: str) -&gt; CondaPackageFormat:\n        \"\"\"Convert string to CondaPackageFormat\n\n        Arguments:\n            name: either the enum name or a file extension, i.e.\n                \"V1\"/\".tar.bz2\", \"V2\"/\".conda\", or \"TREE\"\n        \"\"\"\n        try:\n            return cls[name.upper()]\n        except LookupError:\n            return cls(name.lower())\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaPackageFormat-functions","level":4,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaPackageFormat.from_string","level":5,"title":"from_string  <small><code>classmethod</code></small>","text":"<pre><code>from_string(name: str) -&gt; CondaPackageFormat\n</code></pre> <p>Convert string to CondaPackageFormat</p> PARAMETER DESCRIPTION <code>name</code> <p>either the enum name or a file extension, i.e. \"V1\"/\".tar.bz2\", \"V2\"/\".conda\", or \"TREE\"</p> <p> TYPE: <code>str</code> </p> Source code in <code>src/whl2conda/impl/pyproject.py</code> <pre><code>@classmethod\ndef from_string(cls, name: str) -&gt; CondaPackageFormat:\n    \"\"\"Convert string to CondaPackageFormat\n\n    Arguments:\n        name: either the enum name or a file extension, i.e.\n            \"V1\"/\".tar.bz2\", \"V2\"/\".conda\", or \"TREE\"\n    \"\"\"\n    try:\n        return cls[name.upper()]\n    except LookupError:\n        return cls(name.lower())\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo","level":3,"title":"CondaTargetInfo  <small><code>dataclass</code></small>","text":"<p>Conda package target metadata derived from wheel tags.</p> <p>For noarch (pure python) packages, uses standard noarch values. For platform-specific packages, derives values from wheel platform tags.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>@dataclass(slots=True)\nclass CondaTargetInfo:\n    \"\"\"Conda package target metadata derived from wheel tags.\n\n    For noarch (pure python) packages, uses standard noarch values.\n    For platform-specific packages, derives values from wheel platform tags.\n    \"\"\"\n\n    subdir: str\n    arch: str | None\n    platform: str | None\n    build_string: str\n    is_noarch: bool\n    site_packages_prefix: str\n    python_version: str = \"\"\n    \"\"\"Python version for binary packages (e.g. '3.13'). Empty for noarch.\"\"\"\n    is_abi3: bool = False\n    \"\"\"True for binary packages built against the CPython stable ABI.\"\"\"\n\n    @property\n    def uses_noarch_python(self) -&gt; bool:\n        \"\"\"True if package uses the noarch python install machinery.\n\n        This applies both to noarch packages and to platform-specific\n        abi3 packages (see CEP-20), whose files are placed under\n        ``site-packages/`` and relocated by the installer.\n        \"\"\"\n        return self.is_noarch or self.is_abi3\n\n    def marker_environment(self) -&gt; dict[str, str]:\n        \"\"\"Return PEP 508 marker environment for the target platform.\n\n        Used to evaluate environment markers on dependencies when\n        converting binary wheels.\n        \"\"\"\n        match self.platform:\n            case \"linux\":\n                os_name = \"posix\"\n                sys_platform = \"linux\"\n                platform_system = \"Linux\"\n            case \"osx\":\n                os_name = \"posix\"\n                sys_platform = \"darwin\"\n                platform_system = \"Darwin\"\n            case \"win\":\n                os_name = \"nt\"\n                sys_platform = \"win32\"\n                platform_system = \"Windows\"\n            case _:\n                return {}\n\n        platform_machine = self.arch or \"\"\n\n        return {\n            \"os_name\": os_name,\n            \"sys_platform\": sys_platform,\n            \"platform_system\": platform_system,\n            \"platform_machine\": platform_machine,\n            \"platform_python_implementation\": \"CPython\",\n            \"implementation_name\": \"cpython\",\n            \"python_version\": self.python_version,\n            \"python_full_version\": f\"{self.python_version}.0\",\n            \"implementation_version\": f\"{self.python_version}.0\",\n        }\n\n    @classmethod\n    def from_wheel_metadata(\n        cls,\n        wheel_md: MetadataFromWheel,\n        build_number: int = 0,\n    ) -&gt; CondaTargetInfo:\n        \"\"\"Create from wheel metadata.\n\n        For pure python wheels, returns noarch target info.\n        For platform-specific wheels, derives conda metadata from wheel tags.\n        \"\"\"\n        if wheel_md.is_pure_python:\n            return cls(\n                subdir=\"noarch\",\n                arch=None,\n                platform=None,\n                build_string=\"py_0\",\n                is_noarch=True,\n                site_packages_prefix=\"site-packages\",\n            )\n\n        # Platform-specific package\n        subdir, arch, platform = _parse_platform_tag(wheel_md.platform_tag)\n        python_version = _python_version_from_tag(wheel_md.python_tag)\n        is_abi3 = wheel_md.abi_tag == \"abi3\"\n        py_tag = f\"py{''.join(python_version.split('.'))}\"\n\n        if is_abi3:\n            # Stable ABI package (CEP-20): one build works for all python\n            # versions &gt;= the build version. Files go under site-packages/\n            # and are relocated by the installer like noarch python.\n            build_string = f\"{py_tag}_abi3_{build_number}\"\n            site_packages_prefix = \"site-packages\"\n        else:\n            build_string = f\"{py_tag}_{build_number}\"\n            if platform == \"win\":\n                site_packages_prefix = \"Lib/site-packages\"\n            else:\n                site_packages_prefix = f\"lib/python{python_version}/site-packages\"\n\n        return cls(\n            subdir=subdir,\n            arch=arch,\n            platform=platform,\n            build_string=build_string,\n            is_noarch=False,\n            site_packages_prefix=site_packages_prefix,\n            python_version=python_version,\n            is_abi3=is_abi3,\n        )\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo.is_abi3","level":5,"title":"is_abi3  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>is_abi3: bool = False\n</code></pre> <p>True for binary packages built against the CPython stable ABI.</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo.python_version","level":5,"title":"python_version  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>python_version: str = ''\n</code></pre> <p>Python version for binary packages (e.g. '3.13'). Empty for noarch.</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo.uses_noarch_python","level":5,"title":"uses_noarch_python  <small><code>property</code></small>","text":"<pre><code>uses_noarch_python: bool\n</code></pre> <p>True if package uses the noarch python install machinery.</p> <p>This applies both to noarch packages and to platform-specific abi3 packages (see CEP-20), whose files are placed under <code>site-packages/</code> and relocated by the installer.</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo-functions","level":4,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo.from_wheel_metadata","level":5,"title":"from_wheel_metadata  <small><code>classmethod</code></small>","text":"<pre><code>from_wheel_metadata(\n    wheel_md: MetadataFromWheel, build_number: int = 0\n) -&gt; CondaTargetInfo\n</code></pre> <p>Create from wheel metadata.</p> <p>For pure python wheels, returns noarch target info. For platform-specific wheels, derives conda metadata from wheel tags.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>@classmethod\ndef from_wheel_metadata(\n    cls,\n    wheel_md: MetadataFromWheel,\n    build_number: int = 0,\n) -&gt; CondaTargetInfo:\n    \"\"\"Create from wheel metadata.\n\n    For pure python wheels, returns noarch target info.\n    For platform-specific wheels, derives conda metadata from wheel tags.\n    \"\"\"\n    if wheel_md.is_pure_python:\n        return cls(\n            subdir=\"noarch\",\n            arch=None,\n            platform=None,\n            build_string=\"py_0\",\n            is_noarch=True,\n            site_packages_prefix=\"site-packages\",\n        )\n\n    # Platform-specific package\n    subdir, arch, platform = _parse_platform_tag(wheel_md.platform_tag)\n    python_version = _python_version_from_tag(wheel_md.python_tag)\n    is_abi3 = wheel_md.abi_tag == \"abi3\"\n    py_tag = f\"py{''.join(python_version.split('.'))}\"\n\n    if is_abi3:\n        # Stable ABI package (CEP-20): one build works for all python\n        # versions &gt;= the build version. Files go under site-packages/\n        # and are relocated by the installer like noarch python.\n        build_string = f\"{py_tag}_abi3_{build_number}\"\n        site_packages_prefix = \"site-packages\"\n    else:\n        build_string = f\"{py_tag}_{build_number}\"\n        if platform == \"win\":\n            site_packages_prefix = \"Lib/site-packages\"\n        else:\n            site_packages_prefix = f\"lib/python{python_version}/site-packages\"\n\n    return cls(\n        subdir=subdir,\n        arch=arch,\n        platform=platform,\n        build_string=build_string,\n        is_noarch=False,\n        site_packages_prefix=site_packages_prefix,\n        python_version=python_version,\n        is_abi3=is_abi3,\n    )\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.CondaTargetInfo.marker_environment","level":5,"title":"marker_environment","text":"<pre><code>marker_environment() -&gt; dict[str, str]\n</code></pre> <p>Return PEP 508 marker environment for the target platform.</p> <p>Used to evaluate environment markers on dependencies when converting binary wheels.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def marker_environment(self) -&gt; dict[str, str]:\n    \"\"\"Return PEP 508 marker environment for the target platform.\n\n    Used to evaluate environment markers on dependencies when\n    converting binary wheels.\n    \"\"\"\n    match self.platform:\n        case \"linux\":\n            os_name = \"posix\"\n            sys_platform = \"linux\"\n            platform_system = \"Linux\"\n        case \"osx\":\n            os_name = \"posix\"\n            sys_platform = \"darwin\"\n            platform_system = \"Darwin\"\n        case \"win\":\n            os_name = \"nt\"\n            sys_platform = \"win32\"\n            platform_system = \"Windows\"\n        case _:\n            return {}\n\n    platform_machine = self.arch or \"\"\n\n    return {\n        \"os_name\": os_name,\n        \"sys_platform\": sys_platform,\n        \"platform_system\": platform_system,\n        \"platform_machine\": platform_machine,\n        \"platform_python_implementation\": \"CPython\",\n        \"implementation_name\": \"cpython\",\n        \"python_version\": self.python_version,\n        \"python_full_version\": f\"{self.python_version}.0\",\n        \"implementation_version\": f\"{self.python_version}.0\",\n    }\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.DependencyRename","level":3,"title":"DependencyRename","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Defines a pypi to conda package renaming rule.</p> <p>The pattern must fully match the input package. The replacement string may contain group references e.g. r'\\1', r'\\g`. Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>class DependencyRename(NamedTuple):\n    r\"\"\"\n    Defines a pypi to conda package renaming rule.\n\n    The pattern must fully match the input package.\n    The replacement string may contain group references\n    e.g. r'\\1', r'\\g&lt;name&gt;`.\n    \"\"\"\n\n    pattern: re.Pattern\n    replacement: str\n\n    @classmethod\n    def from_strings(cls, pattern: str, replacement: str) -&gt; DependencyRename:\n        r\"\"\"Construct from strings\n\n        This will also translate '$#' and '${name}' expressions\n        into r'\\#' and r'\\P&lt;name&gt;' respectively.\n        \"\"\"\n        try:\n            pat = re.compile(pattern)\n        except re.error as err:\n            raise ValueError(f\"Bad dependency rename pattern '{pattern}': {err}\")\n        repl = re.sub(r\"\\$(\\d+)\", r\"\\\\\\1\", replacement)\n        repl = re.sub(r\"\\$\\{(\\w+)}\", r\"\\\\g&lt;\\1&gt;\", repl)\n        # TODO also verify replacement does not contain invalid package chars\n        try:\n            pat.sub(repl, \"\")\n        except Exception as ex:\n            if isinstance(ex, re.error):\n                msg = ex.msg\n            else:\n                msg = str(ex)\n            raise ValueError(\n                f\"Bad dependency replacement '{replacement}' for pattern '{pattern}': {msg}\"\n            )\n        return cls(pat, repl)\n\n    def rename(self, pypi_name: str) -&gt; tuple[str, bool]:\n        \"\"\"Rename dependency package name\n\n        The pattern is matched against the name as written and,\n        failing that, its PEP 503 normalized form. The original\n        name is returned unchanged if neither matches.\n\n        Returns conda name and indicator of whether the\n        pattern was applied.\n        \"\"\"\n        for name in (pypi_name, normalize_pypi_name(pypi_name)):\n            if m := self.pattern.fullmatch(name):\n                return m.expand(self.replacement), True\n        return pypi_name, False\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.DependencyRename-functions","level":4,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.DependencyRename.from_strings","level":5,"title":"from_strings  <small><code>classmethod</code></small>","text":"<pre><code>from_strings(\n    pattern: str, replacement: str\n) -&gt; DependencyRename\n</code></pre> <p>Construct from strings</p> <p>This will also translate '$#' and '${name}' expressions into r'#' and r'\\P' respectively. Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>@classmethod\ndef from_strings(cls, pattern: str, replacement: str) -&gt; DependencyRename:\n    r\"\"\"Construct from strings\n\n    This will also translate '$#' and '${name}' expressions\n    into r'\\#' and r'\\P&lt;name&gt;' respectively.\n    \"\"\"\n    try:\n        pat = re.compile(pattern)\n    except re.error as err:\n        raise ValueError(f\"Bad dependency rename pattern '{pattern}': {err}\")\n    repl = re.sub(r\"\\$(\\d+)\", r\"\\\\\\1\", replacement)\n    repl = re.sub(r\"\\$\\{(\\w+)}\", r\"\\\\g&lt;\\1&gt;\", repl)\n    # TODO also verify replacement does not contain invalid package chars\n    try:\n        pat.sub(repl, \"\")\n    except Exception as ex:\n        if isinstance(ex, re.error):\n            msg = ex.msg\n        else:\n            msg = str(ex)\n        raise ValueError(\n            f\"Bad dependency replacement '{replacement}' for pattern '{pattern}': {msg}\"\n        )\n    return cls(pat, repl)\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.DependencyRename.rename","level":5,"title":"rename","text":"<pre><code>rename(pypi_name: str) -&gt; tuple[str, bool]\n</code></pre> <p>Rename dependency package name</p> <p>The pattern is matched against the name as written and, failing that, its PEP 503 normalized form. The original name is returned unchanged if neither matches.</p> <p>Returns conda name and indicator of whether the pattern was applied.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def rename(self, pypi_name: str) -&gt; tuple[str, bool]:\n    \"\"\"Rename dependency package name\n\n    The pattern is matched against the name as written and,\n    failing that, its PEP 503 normalized form. The original\n    name is returned unchanged if neither matches.\n\n    Returns conda name and indicator of whether the\n    pattern was applied.\n    \"\"\"\n    for name in (pypi_name, normalize_pypi_name(pypi_name)):\n        if m := self.pattern.fullmatch(name):\n            return m.expand(self.replacement), True\n    return pypi_name, False\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.MetadataFromWheel","level":3,"title":"MetadataFromWheel  <small><code>dataclass</code></small>","text":"<p>Metadata parsed from wheel distribution</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>@dataclass(slots=True)\nclass MetadataFromWheel:\n    \"\"\"Metadata parsed from wheel distribution\"\"\"\n\n    md: dict[str, Any]\n    package_name: str\n    version: str\n    wheel_build_number: str\n    license: str | None\n    dependencies: list[RequiresDistEntry]\n    wheel_info_dir: Path\n    is_pure_python: bool\n    python_tag: str\n    abi_tag: str\n    platform_tag: str\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry","level":3,"title":"RequiresDistEntry  <small><code>dataclass</code></small>","text":"<p>Requires-Dist metadata entry parsed from wheel</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>@dataclass(slots=True)\nclass RequiresDistEntry:\n    \"\"\"\n    Requires-Dist metadata entry parsed from wheel\n    \"\"\"\n\n    # see https://packaging.python.org/specifications/core-metadata/#requires-dist-multiple-use\n    # and https://peps.python.org/pep-0508/\n    name: str\n    extras: Sequence[str] = ()\n    version: str = \"\"\n    marker: str = \"\"\n\n    extra_marker_name: str = \"\"\n    \"\"\"Name from extra expression in marker, if any\"\"\"\n\n    generic: bool = True\n    \"\"\"True if marker is empty or only contains an extra expression\"\"\"\n\n    def set_marker(self, marker: str) -&gt; None:\n        \"\"\"Set marker value and update extra_marker_name and generic values\"\"\"\n        self.marker = marker\n        self.generic = False\n        for pat in _extra_marker_re:\n            if m := pat.search(marker):\n                self.extra_marker_name = m.group(\"name\")\n                if m.group(0) == marker:\n                    self.generic = True\n                return\n\n    @classmethod\n    def parse(cls, raw: str) -&gt; RequiresDistEntry:\n        \"\"\"\n        Parse entry from raw string read from \"Requires-Dist\" or related header.\n\n        Raises:\n            SyntaxError: if entry is not properly formatted.\n        \"\"\"\n        m = requires_dist_re.fullmatch(raw)\n        if not m:\n            raise SyntaxError(f\"Cannot parse Requires-Dist entry: {raw!r}\")\n        entry = RequiresDistEntry(name=m.group(\"name\"))\n        if extra := m.group(\"extra\"):\n            entry.extras = tuple(re.split(r\"\\s*,\\s*\", extra))\n        if version := m.group(\"version\"):\n            entry.version = version\n        if marker := m.group(\"marker\"):\n            entry.set_marker(marker)\n        return entry\n\n    def with_extra(self, name: str) -&gt; RequiresDistEntry:\n        \"\"\"Returns copy of entry with  an extra == '&lt;name&gt;' clause to marker\"\"\"\n        marker = f\"extra == '{name}'\"\n        if self.marker:\n            marker = f\"({self.marker}) and {marker}\"\n\n        entry = dataclasses.replace(self)\n        entry.set_marker(marker)\n        return entry\n\n    def __str__(self) -&gt; str:\n        with io.StringIO() as buf:\n            buf.write(self.name)\n            if self.extras:\n                buf.write(f\" [{','.join(self.extras)}]\")\n            if self.version:\n                buf.write(f\" {self.version}\")\n            if self.marker:\n                buf.write(f\" ; {self.marker}\")\n            return buf.getvalue()\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry.extra_marker_name","level":5,"title":"extra_marker_name  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>extra_marker_name: str = ''\n</code></pre> <p>Name from extra expression in marker, if any</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry.generic","level":5,"title":"generic  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>generic: bool = True\n</code></pre> <p>True if marker is empty or only contains an extra expression</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry-functions","level":4,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry.parse","level":5,"title":"parse  <small><code>classmethod</code></small>","text":"<pre><code>parse(raw: str) -&gt; RequiresDistEntry\n</code></pre> <p>Parse entry from raw string read from \"Requires-Dist\" or related header.</p> RAISES DESCRIPTION <code>SyntaxError</code> <p>if entry is not properly formatted.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>@classmethod\ndef parse(cls, raw: str) -&gt; RequiresDistEntry:\n    \"\"\"\n    Parse entry from raw string read from \"Requires-Dist\" or related header.\n\n    Raises:\n        SyntaxError: if entry is not properly formatted.\n    \"\"\"\n    m = requires_dist_re.fullmatch(raw)\n    if not m:\n        raise SyntaxError(f\"Cannot parse Requires-Dist entry: {raw!r}\")\n    entry = RequiresDistEntry(name=m.group(\"name\"))\n    if extra := m.group(\"extra\"):\n        entry.extras = tuple(re.split(r\"\\s*,\\s*\", extra))\n    if version := m.group(\"version\"):\n        entry.version = version\n    if marker := m.group(\"marker\"):\n        entry.set_marker(marker)\n    return entry\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry.set_marker","level":5,"title":"set_marker","text":"<pre><code>set_marker(marker: str) -&gt; None\n</code></pre> <p>Set marker value and update extra_marker_name and generic values</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def set_marker(self, marker: str) -&gt; None:\n    \"\"\"Set marker value and update extra_marker_name and generic values\"\"\"\n    self.marker = marker\n    self.generic = False\n    for pat in _extra_marker_re:\n        if m := pat.search(marker):\n            self.extra_marker_name = m.group(\"name\")\n            if m.group(0) == marker:\n                self.generic = True\n            return\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.RequiresDistEntry.with_extra","level":5,"title":"with_extra","text":"<pre><code>with_extra(name: str) -&gt; RequiresDistEntry\n</code></pre> <p>Returns copy of entry with  an extra == '' clause to marker Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def with_extra(self, name: str) -&gt; RequiresDistEntry:\n    \"\"\"Returns copy of entry with  an extra == '&lt;name&gt;' clause to marker\"\"\"\n    marker = f\"extra == '{name}'\"\n    if self.marker:\n        marker = f\"({self.marker}) and {marker}\"\n\n    entry = dataclasses.replace(self)\n    entry.set_marker(marker)\n    return entry\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter","level":3,"title":"Wheel2CondaConverter","text":"<p>Converter supports generation of conda package from a pure python wheel.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>class Wheel2CondaConverter:\n    \"\"\"\n    Converter supports generation of conda package from a pure python wheel.\n\n    \"\"\"\n\n    SUPPORTED_WHEEL_VERSIONS = (\"1.0\",)\n    SUPPORTED_METADATA_VERSIONS: tuple[str, ...] = (\n        \"1.0\",\n        \"1.1\",\n        \"1.2\",\n        \"2.1\",\n        \"2.2\",\n        \"2.3\",\n        \"2.4\",\n        \"2.5\",\n    )\n    MULTI_USE_METADATA_KEYS: frozenset[str] = frozenset({\n        \"Classifier\",\n        \"Dynamic\",\n        \"License-File\",\n        \"Obsoletes\",\n        \"Obsoletes-Dist\",\n        \"Platform\",\n        \"Project-URL\",\n        \"Provides\",\n        \"Provides-Dist\",\n        \"Provides-Extra\",\n        \"Requires\",\n        \"Requires-Dist\",\n        \"Requires-External\",\n        \"Supported-Platform\",\n    })\n\n    package_name: str = \"\"\n    logger: logging.Logger\n    wheel_path: Path\n    out_dir: Path\n    dry_run: bool = False\n    out_format: CondaPackageFormat = CondaPackageFormat.V2\n    overwrite: bool = False\n    keep_pip_dependencies: bool = False\n    dependency_rename: list[DependencyRename]\n    extra_dependencies: list[str]\n    python_version: str = \"\"\n    interactive: bool = False\n    build_number: int | None = None\n    allow_impure: bool = False\n    for_conda_forge: bool = False\n    platform_tag: str = \"\"\n    \"\"\"Wheel platform tag to convert for, when the wheel supports several.\"\"\"\n\n    wheel_md: MetadataFromWheel | None = None\n    conda_target: CondaTargetInfo | None = None\n    conda_pkg_path: Path | None = None\n    std_renames: dict[str, str]\n\n    def __init__(\n        self,\n        wheel_path: Path,\n        out_dir: Path,\n        *,\n        update_std_renames: bool = False,\n    ):\n        self.logger = logging.getLogger(__name__)\n        self.wheel_path = wheel_path\n        self.out_dir = out_dir\n        self.dependency_rename = []\n        self.extra_dependencies = []\n        # TODO - option to ignore this\n        self.std_renames = load_std_renames(update=update_std_renames)\n\n    def convert_all(self) -&gt; list[Path]:\n        \"\"\"\n        Convert wheel to a conda package for every platform it supports.\n\n        Produces one conda package per conda platform (subdir) supported\n        by the wheel, each written into a `&lt;subdir&gt;/` subdirectory of the\n        output directory - conda package file names do not include the\n        platform, so packages for multiple platforms cannot share a\n        directory. A pure python or single-platform wheel produces a\n        single package (under `noarch/` or its platform subdirectory).\n\n        Returns:\n            Paths of the converted conda packages.\n        \"\"\"\n        packages: list[Path] = []\n        saved_out_dir = self.out_dir\n        saved_platform_tag = self.platform_tag\n        try:\n            for subdir, platform_tag in self._platform_groups().items():\n                self.out_dir = Path(saved_out_dir) / subdir\n                self.platform_tag = \"\" if platform_tag == \"any\" else platform_tag\n                packages.append(self.convert())\n        finally:\n            self.out_dir = saved_out_dir\n            self.platform_tag = saved_platform_tag\n        return packages\n\n    def _platform_groups(self) -&gt; dict[str, str]:\n        \"\"\"Preferred wheel platform tag per conda subdir of this wheel.\"\"\"\n        with tempfile.TemporaryDirectory(prefix=\"whl2conda-\") as temp_dirname:\n            extracted_wheel_dir = self._extract_wheel(Path(temp_dirname))\n            info_dir = next(extracted_wheel_dir.glob(\"*.dist-info\"))\n            WHEEL_msg = self.read_metadata_file(info_dir / \"WHEEL\")\n        all_tags = WHEEL_msg.get_all(\"Tag\") or [\"py3-none-any\"]\n\n        result: dict[str, str] = {}\n        for tag in all_tags:\n            if not tag.lower().partition(\"-\")[0].startswith((\"py3\", \"cp3\")):\n                continue\n            platform_tag = tag.lower().rpartition(\"-\")[2]\n            if platform_tag == \"any\":\n                subdir = \"noarch\"\n            else:\n                try:\n                    subdir, _arch, _platform = _parse_platform_tag(platform_tag)\n                except Wheel2CondaError:\n                    subdir = platform_tag\n            existing = result.get(subdir)\n            # prefer an arch-specific tag over universal2 (see\n            # _select_wheel_tag), otherwise keep the wheel's first tag\n            if existing is None or (\n                existing.endswith(\"universal2\")\n                and not platform_tag.endswith(\"universal2\")\n            ):\n                result[subdir] = platform_tag\n        if not result:\n            raise Wheel2CondaError(\n                f\"Wheel {self.wheel_path} has no Python 3 compatible tag\"\n            )\n        return result\n\n    def convert(self) -&gt; Path:\n        \"\"\"\n        Convert wheel to conda package\n\n        Does not write any non-temporary files if dry_run is True.\n\n        Returns:\n            Path of conda package\n        \"\"\"\n        with tempfile.TemporaryDirectory(prefix=\"whl2conda-\") as temp_dirname:\n            temp_dir = Path(temp_dirname)\n            extracted_wheel_dir = self._extract_wheel(temp_dir)\n\n            wheel_md = self._parse_wheel_metadata(extracted_wheel_dir)\n\n            if self.build_number is not None:\n                build_number = self.build_number\n            else:\n                try:\n                    build_number = int(wheel_md.wheel_build_number)\n                except ValueError:\n                    build_number = 0\n\n            conda_target = CondaTargetInfo.from_wheel_metadata(\n                wheel_md, build_number=build_number\n            )\n            self.conda_target = conda_target\n\n            if not conda_target.is_noarch:\n                self._check_binary_conversion(wheel_md)\n\n            conda_dir = temp_dir / \"conda-files\"\n            conda_info_dir = conda_dir.joinpath(\"info\")\n            conda_dir.mkdir()\n\n            # Copy files into conda package\n            self._copy_wheel_files(extracted_wheel_dir, conda_dir)\n\n            # collect relative paths before constructing info/ directory\n            # (posix style - conda package paths always use forward slashes)\n            rel_files = [\n                f.relative_to(conda_dir).as_posix()\n                for f in conda_dir.glob(\"**/*\")\n                if f.is_file()\n            ]\n\n            # For binary packages, evaluate platform markers against target\n            marker_env = (\n                conda_target.marker_environment()\n                if not conda_target.is_noarch\n                else None\n            )\n            conda_dependencies = self._compute_conda_dependencies(\n                wheel_md.dependencies, marker_env=marker_env\n            )\n\n            # Add binary-specific dependencies\n            if not conda_target.is_noarch:\n                self._info(\"Converting non-pure wheel '%s'\", self.wheel_path.name)\n                self._warn_vendored_libraries(rel_files)\n                conda_dependencies = self._add_binary_dependencies(\n                    conda_dependencies, conda_target, wheel_md.platform_tag\n                )\n\n            # Write conda info files\n            # TODO - copy readme file into info\n            #  must be one of README, README.md or README.rst\n            self._copy_licenses(conda_info_dir, wheel_md)\n            self._write_about(conda_info_dir, wheel_md.md)\n            self._write_hash_input(conda_info_dir)\n            self._write_files_list(conda_info_dir, rel_files)\n            self._write_index(\n                conda_info_dir, wheel_md, conda_dependencies, conda_target\n            )\n            self._write_link_file(conda_info_dir, wheel_md, conda_target)\n            self._write_paths_file(conda_dir, rel_files)\n            self._write_git_file(conda_info_dir)\n\n            conda_pkg_path = self._conda_package_path(\n                wheel_md.package_name, wheel_md.version, conda_target\n            )\n            self._write_conda_package(conda_dir, conda_pkg_path)\n\n            return conda_pkg_path\n\n    @classmethod\n    def read_metadata_file(cls, file: Path) -&gt; email.message.Message:\n        \"\"\"\n        Read a wheel email-formatted metadata file (e.g. METADATA, WHEEL)\n\n        Args:\n            file: path to file\n\n        Returns:\n            Message object\n        \"\"\"\n        return email.message_from_string(\n            file.read_text(encoding=\"utf8\", errors=\"backslashreplace\"),\n            policy=email.policy.EmailPolicy(utf8=True, refold_source=\"none\"),  # type: ignore\n        )\n\n    def _conda_package_path(\n        self, package_name: str, version: str, conda_target: CondaTargetInfo\n    ) -&gt; Path:\n        \"\"\"Construct conda package file path\"\"\"\n        if self.out_format is CondaPackageFormat.TREE:\n            suffix = \"\"\n        else:\n            suffix = str(self.out_format.value)\n        conda_pkg_file = f\"{package_name}-{version}-{conda_target.build_string}{suffix}\"\n        self.conda_pkg_path = Path(self.out_dir).joinpath(conda_pkg_file)\n        return self.conda_pkg_path\n\n    def _write_conda_package(self, conda_dir: Path, conda_pkg_path: Path) -&gt; Path:\n        dry_run_suffix = \" (dry run)\" if self.dry_run else \"\"\n        if self.logger.getEffectiveLevel() &lt;= logging.DEBUG:\n            for file in conda_dir.glob(\"**/*\"):\n                if file.is_file():\n                    self._debug(\"Packaging %s\", file.relative_to(conda_dir))\n        if conda_pkg_path.exists():\n            if not self.overwrite:\n                msg = f\"Output conda package already exists at '{conda_pkg_path}'\"\n                overwrite = False\n                if self.interactive:\n                    print(msg)\n                    overwrite = bool_input(\"Overwrite? \")\n                if not overwrite:\n                    raise FileExistsError(msg)\n            self._info(\"Removing existing %s%s\", conda_pkg_path, dry_run_suffix)\n            if not self.dry_run:\n                if conda_pkg_path.is_dir():\n                    shutil.rmtree(conda_pkg_path)\n                else:\n                    conda_pkg_path.unlink()\n        self._info(\"Writing %s%s\", conda_pkg_path, dry_run_suffix)\n\n        if not self.dry_run:\n            if self.out_format is CondaPackageFormat.TREE:\n                shutil.copytree(\n                    conda_dir, Path(self.out_dir).joinpath(conda_pkg_path.name)\n                )\n            else:\n                self.out_dir.mkdir(parents=True, exist_ok=True)\n                create_conda_pkg(conda_dir, None, conda_pkg_path.name, self.out_dir)\n\n        return conda_pkg_path\n\n    def _write_git_file(self, conda_info_dir: Path) -&gt; None:\n        \"\"\"Write empty git file\"\"\"\n        # python wheels don't have this concept, but conda-build\n        # will write an empty git file if there are no git sources,\n        # so we follow suit:\n        conda_info_dir.joinpath(\"git\").write_bytes(b'')\n\n    def _write_paths_file(self, conda_dir: Path, rel_files: Sequence[str]) -&gt; None:\n        # info/paths.json - paths with SHA256 do we really need this?\n        conda_paths_file = conda_dir.joinpath(\"info\", \"paths.json\")\n        paths: list[dict[str, Any]] = []\n        for rel_file in rel_files:\n            abs_file = conda_dir.joinpath(rel_file)\n            file_bytes = abs_file.read_bytes()\n            paths.append({\n                \"_path\": rel_file,\n                \"path_type\": \"hardlink\",\n                \"sha256\": sha256(file_bytes).hexdigest(),\n                \"size_in_bytes\": len(file_bytes),\n            })\n        conda_paths_file.write_text(\n            json.dumps({\"paths\": paths, \"paths_version\": 1}, indent=2), encoding=\"utf8\"\n        )\n\n    def _write_link_file(\n        self,\n        conda_info_dir: Path,\n        wheel_md: MetadataFromWheel,\n        conda_target: CondaTargetInfo,\n    ) -&gt; None:\n        # Binary packages don't use link.json (matches conda-forge convention),\n        # except abi3 packages, which use the noarch python install machinery\n        if not conda_target.uses_noarch_python:\n            return\n\n        # info/link.json\n        conda_link_file = conda_info_dir.joinpath(\"link.json\")\n        wheel_entry_points_file = wheel_md.wheel_info_dir.joinpath(\"entry_points.txt\")\n        console_scripts: list[str] = []\n        if wheel_entry_points_file.is_file():\n            wheel_entry_points = configparser.ConfigParser()\n            wheel_entry_points.read(wheel_entry_points_file)\n            for section_name in [\"console_scripts\", \"gui_scripts\"]:\n                if section_name in wheel_entry_points:\n                    section = wheel_entry_points[section_name]\n                    console_scripts.extend(f\"{k}={v}\" for k, v in section.items())\n\n        link_dict: dict[str, Any] = {\"package_metadata_version\": 1}\n        noarch_dict: dict[str, Any] = {\"type\": \"python\"}\n        if console_scripts:\n            noarch_dict[\"entry_points\"] = console_scripts\n        link_dict[\"noarch\"] = noarch_dict\n\n        conda_link_file.write_text(\n            json.dumps(link_dict, indent=2, sort_keys=True),\n            encoding=\"utf8\",\n        )\n\n    def _write_index(\n        self,\n        conda_info_dir: Path,\n        wheel_md: MetadataFromWheel,\n        conda_dependencies: Sequence[str],\n        conda_target: CondaTargetInfo,\n    ) -&gt; None:\n        # info/index.json\n        conda_index_file = conda_info_dir.joinpath(\"index.json\")\n\n        if self.build_number is not None:\n            build_number = self.build_number\n        else:\n            try:\n                build_number = int(wheel_md.wheel_build_number)\n            except ValueError:\n                build_number = 0\n\n        index_dict: dict[str, Any] = {\n            \"arch\": conda_target.arch,\n            \"build\": conda_target.build_string,\n            \"build_number\": build_number,\n            \"depends\": conda_dependencies,\n            \"license\": wheel_md.license,\n            \"name\": wheel_md.package_name,\n            \"platform\": conda_target.platform,\n            \"subdir\": conda_target.subdir,\n            \"timestamp\": int(time.time() * 1000),  # milliseconds since epoch\n            \"version\": wheel_md.version,\n        }\n        if conda_target.uses_noarch_python:\n            # Set for abi3 packages too, which keep their platform subdir\n            # but use the noarch python install machinery (CEP-20)\n            index_dict[\"noarch\"] = \"python\"\n\n        conda_index_file.write_text(\n            json.dumps(index_dict, indent=2),\n            encoding=\"utf8\",\n        )\n\n    # Platform tag mapping is now handled by _WHEEL_PLATFORM_MAP and\n    # CondaTargetInfo.from_wheel_metadata()\n\n    def _write_files_list(self, conda_info_dir: Path, rel_files: Sequence[str]) -&gt; None:\n        # * info/files - list of relative paths of files not including info/\n        conda_files_file = conda_info_dir.joinpath(\"files\")\n        with open(conda_files_file, \"w\", encoding=\"utf8\") as f:\n            for rel_file in rel_files:\n                f.write(str(rel_file))\n                f.write(\"\\n\")\n\n    def _write_hash_input(self, conda_info_dir: Path) -&gt; None:\n        conda_hash_input_file = conda_info_dir.joinpath(\"hash_input.json\")\n        conda_hash_input_file.write_text(json.dumps({}, indent=2), encoding=\"utf8\")\n\n    def _write_about(self, conda_info_dir: Path, md: dict[str, Any]) -&gt; None:\n        \"\"\"Write the info/about.json file\"\"\"\n        # * info/about.json\n        #\n        # Note that the supported fields in the about section are not\n        # well documented, but conda-build will only copy fields from\n        # its approved list, which can be found in the FIELDS datastructure\n        # in the conda_build.metadata module. This currently includes:\n        #\n        #   URLS: home, dev_url, doc_url, doc_source_url\n        #   Text: license, summary, description, license_family\n        #   Lists: tags, keyword\n        #   Paths in source tree: license-file, prelink_message, readme\n        #\n        # conda-build also adds conda-build-version and conda-version fields.\n\n        # TODO description can come from METADATA message body\n        #   then need to also use content type. It doesn't seem\n        #   that conda-forge packages include this in the info/\n\n        conda_about_file = conda_info_dir.joinpath(\"about.json\")\n\n        extra = non_none_dict(\n            author=md.get(\"author\"),\n            classifiers=md.get(\"classifier\"),\n            maintainer=md.get(\"maintainer\"),\n            whl2conda_version=__version__,\n        )\n\n        proj_url_pat = re.compile(r\"\\s*(?P&lt;key&gt;\\w+(\\s+\\w+)*)\\s*,\\s*(?P&lt;url&gt;\\w.*)\\s*\")\n        doc_url: str | None = None\n        dev_url: str | None = None\n        for urlline in md.get(\"project-url\", ()):\n            if m := proj_url_pat.match(urlline):  # pragma: no branch\n                key = m.group(\"key\")\n                url = m.group(\"url\")\n                if re.match(r\"(?i)doc(umentation)?\\b\", key):\n                    doc_url = url\n                elif re.match(r\"(?i)(dev(elopment)?|repo(sitory))\\b\", key):\n                    dev_url = url\n                extra[key] = url\n\n        for key in [\"author-email\", \"maintainer-email\"]:\n            val = md.get(key)\n            if val:\n                author_key = key.split(\"-\", maxsplit=1)[0] + \"s\"\n                extra[author_key] = val.split(\",\")\n\n        license = md.get(\"license-expression\") or md.get(\"license\")\n        if license_files := md.get(\"license-file\"):\n            extra[\"license_files\"] = list(license_files)\n\n        if keywords := md.get(\"keywords\"):\n            keyword_list = keywords.split(\",\")\n        else:\n            keyword_list = None\n\n        conda_about_file.write_text(\n            json.dumps(\n                non_none_dict(\n                    description=md.get(\"description\"),\n                    summary=md.get(\"summary\"),\n                    license=license or None,\n                    keywords=keyword_list,\n                    home=md.get(\"home-page\"),\n                    dev_url=dev_url,\n                    doc_url=doc_url,\n                    extra=extra,\n                ),\n                indent=2,\n                sort_keys=True,\n            ),\n            encoding=\"utf8\",\n        )\n\n    def _compute_conda_dependencies(\n        self,\n        dependencies: Sequence[RequiresDistEntry],\n        marker_env: dict[str, str] | None = None,\n    ) -&gt; list[str]:\n        conda_dependencies: list[str] = []\n\n        saw_python = False\n\n        for entry in dependencies:\n            if entry.extra_marker_name:\n                self._debug(\"Skipping extra dependency: %s\", entry)\n                continue\n            if not entry.generic:\n                if marker_env:\n                    # Evaluate marker against target platform\n                    if not _evaluate_marker(entry.marker, marker_env):\n                        self._debug(\n                            \"Skipping dependency (marker not satisfied): %s\", entry\n                        )\n                        continue\n                    self._debug(\n                        \"Including marker dependency for target platform: %s\", entry\n                    )\n                else:\n                    # TODO - support non-generic packages\n                    self._warn(\"Skipping dependency with environment marker: %s\", entry)\n                    continue\n\n            conda_name = pip_name = entry.name\n            version = self.translate_version_spec(entry.version)\n            if saw_python := normalize_pypi_name(conda_name) == \"python\":\n                if self.python_version and version != self.python_version:\n                    self._info(\n                        \"Overriding python version '%s' with '%s'\",\n                        version,\n                        self.python_version,\n                    )\n                    version = self.python_version\n\n            # TODO - do something with extras (#36)\n            #   download target pip package and its extra dependencies\n            # check manual renames first\n            renamed = False\n            for renamer in self.dependency_rename:\n                conda_name, renamed = renamer.rename(pip_name)\n                if renamed:\n                    break\n            if not renamed:\n                conda_name = self.std_renames.get(\n                    normalize_pypi_name(pip_name), pip_name\n                )\n\n            if conda_name:\n                conda_dep = f\"{conda_name} {version}\"\n                if conda_name == pip_name:\n                    self._debug(\"Dependency copied: '%s'\", conda_dep)\n                else:\n                    self._debug(\"Dependency renamed: '%s' -&gt; '%s'\", entry, conda_dep)\n                conda_dependencies.append(conda_dep)\n            else:\n                self._debug(\"Dependency dropped: %s\", entry)\n\n        if not saw_python and self.python_version:\n            self._info(\"Added 'python %s' dependency\", self.python_version)\n            conda_dependencies.append(f\"python {self.python_version}\")\n\n        for dep in self.extra_dependencies:\n            self._debug(\"Dependency added:  '%s'\", dep)\n            conda_dependencies.append(dep)\n        return conda_dependencies\n\n    def _add_binary_dependencies(\n        self,\n        conda_dependencies: list[str],\n        conda_target: CondaTargetInfo,\n        platform_tag: str,\n    ) -&gt; list[str]:\n        \"\"\"Add binary-specific dependencies (python pin, OS constraint).\n\n        Replaces any loose python version spec with a tight pin derived from\n        the wheel's Python tag, and adds OS minimum version constraints.\n\n        For abi3 (stable ABI) wheels, only a minimum python version derived\n        from the wheel's Python tag is required, since the package works on\n        all later versions. If `for_conda_forge` is set, the CEP-20 pins\n        used by conda-forge (`cpython` and `_python_abi3_support`) are also\n        added for abi3 wheels.\n\n        An explicit python version setting on the converter overrides the\n        automatically derived pin.\n        \"\"\"\n        result = list(conda_dependencies)\n        abi3_python_spec = \"\"\n        if self.python_version:\n            # User override was already applied in _compute_conda_dependencies\n            self._debug(\n                \"Binary python pin overridden by user: python %s\",\n                self.python_version,\n            )\n            abi3_python_spec = self.python_version\n        elif conda_target.is_abi3:\n            # Stable ABI: works on all versions &gt;= the build version, so\n            # only add a floor. Keep any Requires-Python constraint, which\n            # may be tighter.\n            abi3_python_spec = f\"&gt;={conda_target.python_version}\"\n            floor = f\"python {abi3_python_spec}\"\n            if floor not in result:\n                result.append(floor)\n            self._debug(\"Binary abi3 python floor: %s\", floor)\n        elif python_pin := _python_pin_from_version(conda_target.python_version):\n            # Remove any existing loose python dependency\n            result = [dep for dep in result if not dep.startswith(\"python \")]\n            result.extend(python_pin)\n            self._debug(\n                \"Binary python pin: %s\",\n                \", \".join(python_pin),\n            )\n\n        if conda_target.is_abi3 and self.for_conda_forge:\n            # CEP-20 pins used by conda-forge: `cpython` excludes PyPy, and\n            # `_python_abi3_support` additionally excludes free-threaded\n            # builds, neither of which support the stable ABI. These\n            # packages only exist on the conda-forge channel.\n            cpython_pin = f\"cpython {abi3_python_spec}\".rstrip()\n            result.append(cpython_pin)\n            result.append(\"_python_abi3_support 1.*\")\n            self._debug(\n                \"conda-forge abi3 pins: %s, _python_abi3_support 1.*\", cpython_pin\n            )\n\n        if os_constraint := _os_constraint_from_platform_tag(platform_tag):\n            result.append(os_constraint)\n            self._debug(\"OS constraint: %s\", os_constraint)\n\n        return result\n\n    # Known package prefixes that are unlikely to work as binary conversions\n    # due to bundled GPU libraries, complex runtime dependencies, etc.\n    # directories used by wheel repair tools (auditwheel, delocate,\n    # delvewheel) to vendor shared libraries into the wheel\n    _VENDORED_LIB_DIR_RE = re.compile(r\"(?:^|/)(?P&lt;dir&gt;[^/]+\\.libs|\\.dylibs)/\")\n\n    def _warn_vendored_libraries(self, rel_files: Sequence[str]) -&gt; None:\n        \"\"\"Warn if the wheel vendors shared libraries.\n\n        Wheels repaired by auditwheel/delocate/delvewheel bundle copies\n        of the shared libraries they link against. The converted conda\n        package will use those bundled copies, unlike an equivalent\n        conda-forge package, which would declare shared library\n        dependencies instead.\n        \"\"\"\n        vendored = sorted({\n            m.group(\"dir\")\n            for relpath in rel_files\n            if (m := self._VENDORED_LIB_DIR_RE.search(relpath))\n        })\n        if vendored:\n            self._warn(\n                \"Wheel bundles shared libraries (%s). The converted package \"\n                \"will use these bundled copies, which may conflict with or \"\n                \"duplicate libraries provided by conda packages.\",\n                \", \".join(vendored),\n            )\n\n    def _check_binary_conversion(self, wheel_md: MetadataFromWheel) -&gt; None:\n        \"\"\"Check for conditions that make binary conversion unlikely to succeed.\n\n        Raises:\n            Wheel2CondaError: if conversion is blocked due to known-bad patterns\n        \"\"\"\n        version = wheel_md.version\n\n        # Check for local version suffix (e.g. +cu121, +rocm5.6)\n        if \"+\" in version:\n            local = version.split(\"+\", 1)[1]\n            raise Wheel2CondaError(\n                f\"Wheel {self.wheel_path.name} has local version suffix '+{local}' \"\n                f\"indicating a custom build variant (e.g. CUDA). \"\n                f\"Such wheels bundle variant-specific libraries that are unlikely \"\n                f\"to work correctly as conda packages. Use conda-forge packages instead.\"\n            )\n\n    def _copy_wheel_files(self, wheel_dir: Path, conda_dir: Path) -&gt; None:\n        \"\"\"\n        Copies files from wheels to corresponding location in conda package:\n\n        For noarch packages:\n        - &lt;wheel-dir&gt;/*.data/data/* -&gt; &lt;conda-dir&gt;/*\n        - &lt;wheel-dir&gt;/*.data/scripts/* -&gt; &lt;conda-dir&gt;/python-scripts/*\n        - &lt;wheel-dir&gt;/*.data/* -&gt; ignored\n        - &lt;wheel-dir&gt;/* -&gt; &lt;conda-dir&gt;/site-packages\n\n        For platform-specific packages:\n        - &lt;wheel-dir&gt;/* -&gt; &lt;conda-dir&gt;/lib/pythonX.Y/site-packages (Unix)\n        - &lt;wheel-dir&gt;/* -&gt; &lt;conda-dir&gt;/Lib/site-packages (Windows)\n        - &lt;wheel-dir&gt;/*.data/scripts/* -&gt; &lt;conda-dir&gt;/bin (Unix)\n        - &lt;wheel-dir&gt;/*.data/scripts/* -&gt; &lt;conda-dir&gt;/Scripts (Windows)\n\n        Platform-specific abi3 packages use the noarch layout, since they\n        are installed using the noarch python machinery (CEP-20).\n        \"\"\"\n        assert self.conda_target is not None\n        target = self.conda_target\n\n        conda_site_packages = conda_dir.joinpath(target.site_packages_prefix)\n        conda_site_packages.mkdir(parents=True)\n        conda_info_dir = conda_dir.joinpath(\"info\")\n        conda_info_dir.mkdir()\n\n        if target.uses_noarch_python:\n            scripts_dest = \"python-scripts\"\n        elif target.platform == \"win\":\n            scripts_dest = \"Scripts\"\n        else:\n            scripts_dest = \"bin\"\n\n        for entry in wheel_dir.iterdir():\n            if not entry.is_dir():\n                shutil.copyfile(entry, conda_site_packages / entry.name)\n            elif not entry.name.endswith(\".data\"):\n                shutil.copytree(\n                    entry, conda_site_packages / entry.name, dirs_exist_ok=True\n                )\n            else:\n                for datapath in entry.iterdir():\n                    if not datapath.is_dir():\n                        self._warn(\n                            \"Do not support top level file '%s' in '%s' directory - ignored\",\n                            datapath.name,\n                            entry.relative_to(wheel_dir),\n                        )\n                        continue\n                    if datapath.name == \"data\":\n                        data_dest = conda_dir\n                    elif datapath.name == \"scripts\":\n                        data_dest = conda_dir / scripts_dest\n                    else:\n                        self._warn(\n                            \"Do not support '%s' path in '%s' directory - ignored\",\n                            datapath.name,\n                            entry.relative_to(wheel_dir),\n                        )\n                        continue\n                    shutil.copytree(datapath, data_dest, dirs_exist_ok=True)\n\n        assert self.wheel_md is not None\n        dist_info_dir = conda_site_packages / self.wheel_md.wheel_info_dir.name\n        installer_file = dist_info_dir / \"INSTALLER\"\n        installer_file.write_text(\"whl2conda\", encoding=\"utf8\")\n        requested_file = dist_info_dir / \"REQUESTED\"\n        requested_file.write_text(\"\", encoding=\"utf8\")\n\n    def _copy_licenses(self, conda_info_dir: Path, wheel_md: MetadataFromWheel) -&gt; None:\n        to_license_dir = conda_info_dir / \"licenses\"\n        wheel_info_dir = wheel_md.wheel_info_dir\n        wheel_license_dir = wheel_info_dir / \"licenses\"\n        if wheel_license_dir.is_dir():\n            # just copy directory\n            shutil.copytree(\n                wheel_license_dir,\n                to_license_dir,\n                dirs_exist_ok=True,\n            )\n        else:\n            # Otherwise look for files in the dist-info dir\n            # that match the license-file entries. The paths\n            # of the license-file entries may be relative to\n            # where the wheel was built and may not directly\n            # point at the files.\n            for license_file in wheel_md.md.get(\"license-file\", ()):\n                # copy license file if it exists\n                license_path = Path(license_file)\n                from_files = [wheel_info_dir / license_path.name]\n                if not license_path.is_absolute():\n                    from_files.insert(0, wheel_info_dir / license_path)\n                for from_file in filter(  # pragma: no branch\n                    lambda f: f.exists(), from_files\n                ):\n                    to_file = to_license_dir / from_file.relative_to(wheel_info_dir)\n                    if not to_file.exists():  # pragma: no branch\n                        to_file.parent.mkdir(parents=True, exist_ok=True)\n                        shutil.copyfile(from_file, to_file)\n                        break\n\n    def _parse_wheel_metadata(self, wheel_dir: Path) -&gt; MetadataFromWheel:\n        \"\"\"Parse all metadata from an extracted wheel directory.\"\"\"\n        wheel_info_dir = next(wheel_dir.glob(\"*.dist-info\"))\n        is_pure_lib, wheel_build_number, python_tag, abi_tag, platform_tag = (\n            self._parse_wheel_info(wheel_info_dir)\n        )\n        md, requires = self._parse_dist_metadata(wheel_info_dir)\n\n        package_name = self.package_name or str(md.get(\"name\"))\n        # Conda package names are lowercase with hyphens\n        package_name = re.sub(r\"[-_.]+\", \"-\", package_name).lower()\n        self.package_name = package_name\n        version = md.get(\"version\")\n\n        # RECORD_file = wheel_info_dir / \"RECORD\"\n        # TODO: strip __pycache__ entries from RECORD\n        # TODO: add INSTALLER and REQUESTED to RECORD\n        # TODO: add direct_url to wheel and to RECORD\n        # RECORD line format: &lt;path&gt;,sha256=&lt;hash&gt;,&lt;len&gt;\n\n        python_version: str = str(md.get(\"requires-python\", \"\"))\n        if python_version:\n            requires.append(RequiresDistEntry(\"python\", version=python_version))\n        self.wheel_md = MetadataFromWheel(\n            md=md,\n            package_name=package_name,\n            version=str(version),\n            wheel_build_number=wheel_build_number,\n            license=md.get(\"license-expression\") or md.get(\"license\"),  # type: ignore\n            dependencies=requires,\n            wheel_info_dir=wheel_info_dir,\n            is_pure_python=is_pure_lib,\n            python_tag=python_tag,\n            abi_tag=abi_tag,\n            platform_tag=platform_tag,\n        )\n        return self.wheel_md\n\n    def _parse_wheel_info(\n        self, wheel_info_dir: Path\n    ) -&gt; tuple[bool, str, str, str, str]:\n        \"\"\"Parse the WHEEL metadata file.\n\n        Returns:\n            Tuple of (is_pure_lib, build_number, python_tag, abi_tag, platform_tag)\n        \"\"\"\n        WHEEL_file = wheel_info_dir.joinpath(\"WHEEL\")\n        WHEEL_msg = self.read_metadata_file(WHEEL_file)\n        # https://peps.python.org/pep-0427/#what-s-the-deal-with-purelib-vs-platlib\n\n        is_pure_lib = WHEEL_msg.get(\"Root-Is-Purelib\", \"\").lower() == \"true\"\n        wheel_build_number = WHEEL_msg.get(\"Build\", \"\")\n        wheel_version = WHEEL_msg.get(\"Wheel-Version\")\n        # Tag entry can appear more than once (e.g. py2-none-any, py3-none-any)\n        all_tags = WHEEL_msg.get_all(\"Tag\") or [\"py3-none-any\"]\n\n        if wheel_version not in self.SUPPORTED_WHEEL_VERSIONS:\n            raise Wheel2CondaError(\n                f\"Wheel {self.wheel_path} has unsupported wheel version {wheel_version}\"\n            )\n\n        # Pick the best py3-compatible tag\n        py3_tags = [\n            tag\n            for tag in all_tags\n            if tag.lower().partition(\"-\")[0].startswith((\"py3\", \"cp3\"))\n        ]\n        if not py3_tags:\n            raise Wheel2CondaError(\n                f\"Wheel {self.wheel_path} has no Python 3 compatible tag\"\n            )\n        wheel_tag = self._select_wheel_tag(py3_tags)\n\n        if not self.allow_impure:\n            if not is_pure_lib:\n                raise Wheel2CondaError(f\"Wheel {self.wheel_path} is not pure python\")\n            if not any(t.lower() == \"py3-none-any\" for t in all_tags):\n                raise Wheel2CondaError(\n                    f\"Wheel {self.wheel_path} has unexpected tag '{wheel_tag}' for pure python\"\n                )\n\n        wheel_tags = wheel_tag.split(\"-\")\n        if len(wheel_tags) != 3:\n            raise Wheel2CondaError(\n                f\"Wheel {self.wheel_path} has bad tag format '{wheel_tags}'\"\n            )\n\n        python_tag, abi_tag, platform_tag = wheel_tags\n        return is_pure_lib, wheel_build_number, python_tag, abi_tag, platform_tag\n\n    def _select_wheel_tag(self, tags: list[str]) -&gt; str:\n        \"\"\"Select the wheel tag to convert for.\n\n        Multi-platform (\"fat\") wheels carry one tag per supported\n        platform. If the `platform_tag` attribute is set, the matching\n        tag is used. Otherwise, the tag matching the current platform\n        is preferred, then a `universal2` tag (which is converted for\n        osx-arm64), and otherwise the wheel's first tag.\n\n        Args:\n            tags: the wheel's python-3 compatible tags, in wheel order\n\n        Returns:\n            The selected tag.\n\n        Raises:\n            Wheel2CondaError: if the `platform_tag` attribute does not\n                match any of the wheel's tags.\n        \"\"\"\n\n        def tag_platform(tag: str) -&gt; str:\n            return tag.lower().rpartition(\"-\")[2]\n\n        if override := self.platform_tag.lower():\n            for tag in tags:\n                if tag_platform(tag) == override:\n                    return tag\n            platforms = sorted({tag_platform(tag) for tag in tags})\n            raise Wheel2CondaError(\n                f\"Wheel {self.wheel_path} has no platform tag\"\n                f\" '{self.platform_tag}' - available: {', '.join(platforms)}\"\n            )\n\n        if len(tags) == 1:\n            return tags[0]\n\n        def prefer_arch_specific(group: list[str]) -&gt; str:\n            # a universal2 tag pairs with an arch-specific tag for the\n            # same subdir, whose __osx version floor is more accurate\n            for tag in group:\n                if not tag_platform(tag).endswith(\"universal2\"):\n                    return tag\n            return group[0]\n\n        # group the tags by their conda subdir, keeping unsupported\n        # platform tags in their own groups\n        subdirs: dict[str, list[str]] = {}\n        for tag in tags:\n            try:\n                subdir, _arch, _platform = _parse_platform_tag(tag_platform(tag))\n            except Wheel2CondaError:\n                subdir = tag_platform(tag)\n            subdirs.setdefault(subdir, []).append(tag)\n\n        if len(subdirs) == 1:\n            return prefer_arch_specific(tags)\n\n        # A fat wheel spanning multiple conda subdirs: prefer the\n        # current platform, then a universal2 tag (osx-arm64).\n        chosen = \"\"\n        native = native_conda_subdir()\n        if native in subdirs:\n            chosen = native\n        else:\n            for subdir, group in subdirs.items():\n                if any(tag_platform(tag).endswith(\"universal2\") for tag in group):\n                    chosen = subdir\n                    break\n        if not chosen:\n            chosen = next(iter(subdirs))\n        self._warn(\n            \"Wheel supports multiple platforms (%s); converting for %s. \"\n            \"Use --platform-tag to override.\",\n            \", \".join(sorted(subdirs)),\n            chosen,\n        )\n        return prefer_arch_specific(subdirs[chosen])\n\n    def _parse_dist_metadata(\n        self, wheel_info_dir: Path\n    ) -&gt; tuple[dict[str, Any], list[RequiresDistEntry]]:\n        \"\"\"Parse the METADATA file and optionally rewrite pip dependencies.\n\n        Returns:\n            Tuple of (metadata_dict, requires_list)\n        \"\"\"\n        wheel_md_file = wheel_info_dir.joinpath(\"METADATA\")\n        md: dict[str, str | list[Any]] = {}\n        # Metadata spec: https://packaging.python.org/en/latest/specifications/core-metadata/\n        # Required keys: Metadata-Version, Name, Version\n        md_msg = self.read_metadata_file(wheel_md_file)\n        md_version_str = md_msg.get(\"Metadata-Version\")\n        if md_version_str not in self.SUPPORTED_METADATA_VERSIONS:\n            msg = f\"Wheel {self.wheel_path} has unsupported metadata version {md_version_str}\"\n            # TODO - perhaps just warn about this if not in \"strict\" mode\n            raise Wheel2CondaError(msg)\n        for mdkey, mdval in md_msg.items():\n            mdkey = mdkey.strip()\n            if mdkey in self.MULTI_USE_METADATA_KEYS:\n                if curmdval := md.get(mdkey):\n                    if isinstance(curmdval, str):\n                        md[mdkey] = [curmdval]\n                md.setdefault(mdkey.lower(), []).append(mdval)  # type: ignore\n            else:\n                md[mdkey.lower()] = mdval\n\n        requires: list[RequiresDistEntry] = []\n        raw_requires_entries = md.get(\"requires-dist\", md.get(\"requires\", ()))\n        for raw_entry in raw_requires_entries:\n            try:\n                entry = RequiresDistEntry.parse(raw_entry)\n                requires.append(entry)\n            except SyntaxError as err:\n                # TODO: error in strict mode?\n                self._warn(str(err))\n\n        if not self.keep_pip_dependencies:\n            # Turn requirements into optional extra requirements\n            del md_msg[\"Requires\"]\n            del md_msg[\"Requires-Dist\"]\n            for entry in requires:\n                if not entry.extra_marker_name:\n                    entry = entry.with_extra('original')\n                md_msg.add_header(\"Requires-Dist\", str(entry))\n            md_msg.add_header(\"Provides-Extra\", \"original\")\n            wheel_md_file.write_text(md_msg.as_string(), encoding=\"utf8\")\n\n        return md, requires\n\n    def translate_version_spec(self, pip_version: str) -&gt; str:\n        \"\"\"\n        Convert a pip version spec to a conda version spec.\n\n        Compatible release specs using the `~=` operator will be turned\n        into two clauses using \"&gt;=\" and \"==\", for example\n        `~=1.2.3` will become `&gt;=1.2.3,1.2.*`.\n\n        Arbitrary equality clauses using the `===` operator will be\n        converted to use `==`, but such clauses are likely to fail\n        so a warning will be produced.\n\n        Any leading \"v\" character in the version will be dropped.\n        (e.g. `v1.2.3` changes to `1.2.3`).\n        \"\"\"\n        pip_version = pip_version.strip()\n        version_specs = re.split(r\"\\s*,\\s*\", pip_version)\n        for i, spec in enumerate(version_specs):\n            if not spec:\n                continue\n            # spec for '~= &lt;version&gt;'\n            # https://packaging.python.org/en/latest/specifications/version-specifiers/#compatible-release\n            if m := pip_version_re.match(spec):\n                operator = m.group(\"operator\")\n                v = m.group(\"version\")\n                v = v.removeprefix(\"v\")\n                if operator == \"~=\":\n                    # compatible operator, e.g. convert ~=1.2.3 to &gt;=1.2.3,==1.2.*\n                    rv = m.group(\"release\")\n                    rv_parts = rv.split(\".\")\n                    operator = \"&gt;=\"\n                    if len(rv_parts) &gt; 1:\n                        # technically ~=1 is not valid, but if we see it, turn it into &gt;=1\n                        v += f\",=={'.'.join(rv_parts[:-1])}.*\"\n                elif operator == \"===\":\n                    operator = \"==\"\n                    # TODO perhaps treat as an error in \"strict\" mode\n                    self._warn(\n                        \"Converted arbitrary equality clause %s to ==%s - may not match!\",\n                        spec,\n                        v,\n                    )\n                version_specs[i] = f\"{operator}{v}\"\n            else:\n                self._warn(\"Cannot convert bad version spec: '%s'\", spec)\n\n        return \",\".join(filter(bool, version_specs))\n\n    def _extract_wheel(self, temp_dir: Path) -&gt; Path:\n        self.logger.info(\"Reading %s\", self.wheel_path)\n        wheel_dir = temp_dir / \"wheel-files\"\n        unpack_wheel(self.wheel_path, wheel_dir, logger=self.logger)\n        return wheel_dir\n\n    def _warn(self, msg, *args):\n        self.logger.warning(msg, *args)\n\n    def _info(self, msg, *args):\n        self.logger.info(msg, *args)\n\n    def _debug(self, msg, *args):\n        self.logger.debug(msg, *args)\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter.platform_tag","level":5,"title":"platform_tag  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>platform_tag: str = ''\n</code></pre> <p>Wheel platform tag to convert for, when the wheel supports several.</p>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter-functions","level":4,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter.convert","level":5,"title":"convert","text":"<pre><code>convert() -&gt; Path\n</code></pre> <p>Convert wheel to conda package</p> <p>Does not write any non-temporary files if dry_run is True.</p> RETURNS DESCRIPTION <code>Path</code> <p>Path of conda package</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def convert(self) -&gt; Path:\n    \"\"\"\n    Convert wheel to conda package\n\n    Does not write any non-temporary files if dry_run is True.\n\n    Returns:\n        Path of conda package\n    \"\"\"\n    with tempfile.TemporaryDirectory(prefix=\"whl2conda-\") as temp_dirname:\n        temp_dir = Path(temp_dirname)\n        extracted_wheel_dir = self._extract_wheel(temp_dir)\n\n        wheel_md = self._parse_wheel_metadata(extracted_wheel_dir)\n\n        if self.build_number is not None:\n            build_number = self.build_number\n        else:\n            try:\n                build_number = int(wheel_md.wheel_build_number)\n            except ValueError:\n                build_number = 0\n\n        conda_target = CondaTargetInfo.from_wheel_metadata(\n            wheel_md, build_number=build_number\n        )\n        self.conda_target = conda_target\n\n        if not conda_target.is_noarch:\n            self._check_binary_conversion(wheel_md)\n\n        conda_dir = temp_dir / \"conda-files\"\n        conda_info_dir = conda_dir.joinpath(\"info\")\n        conda_dir.mkdir()\n\n        # Copy files into conda package\n        self._copy_wheel_files(extracted_wheel_dir, conda_dir)\n\n        # collect relative paths before constructing info/ directory\n        # (posix style - conda package paths always use forward slashes)\n        rel_files = [\n            f.relative_to(conda_dir).as_posix()\n            for f in conda_dir.glob(\"**/*\")\n            if f.is_file()\n        ]\n\n        # For binary packages, evaluate platform markers against target\n        marker_env = (\n            conda_target.marker_environment()\n            if not conda_target.is_noarch\n            else None\n        )\n        conda_dependencies = self._compute_conda_dependencies(\n            wheel_md.dependencies, marker_env=marker_env\n        )\n\n        # Add binary-specific dependencies\n        if not conda_target.is_noarch:\n            self._info(\"Converting non-pure wheel '%s'\", self.wheel_path.name)\n            self._warn_vendored_libraries(rel_files)\n            conda_dependencies = self._add_binary_dependencies(\n                conda_dependencies, conda_target, wheel_md.platform_tag\n            )\n\n        # Write conda info files\n        # TODO - copy readme file into info\n        #  must be one of README, README.md or README.rst\n        self._copy_licenses(conda_info_dir, wheel_md)\n        self._write_about(conda_info_dir, wheel_md.md)\n        self._write_hash_input(conda_info_dir)\n        self._write_files_list(conda_info_dir, rel_files)\n        self._write_index(\n            conda_info_dir, wheel_md, conda_dependencies, conda_target\n        )\n        self._write_link_file(conda_info_dir, wheel_md, conda_target)\n        self._write_paths_file(conda_dir, rel_files)\n        self._write_git_file(conda_info_dir)\n\n        conda_pkg_path = self._conda_package_path(\n            wheel_md.package_name, wheel_md.version, conda_target\n        )\n        self._write_conda_package(conda_dir, conda_pkg_path)\n\n        return conda_pkg_path\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter.convert_all","level":5,"title":"convert_all","text":"<pre><code>convert_all() -&gt; list[Path]\n</code></pre> <p>Convert wheel to a conda package for every platform it supports.</p> <p>Produces one conda package per conda platform (subdir) supported by the wheel, each written into a <code>&lt;subdir&gt;/</code> subdirectory of the output directory - conda package file names do not include the platform, so packages for multiple platforms cannot share a directory. A pure python or single-platform wheel produces a single package (under <code>noarch/</code> or its platform subdirectory).</p> RETURNS DESCRIPTION <code>list[Path]</code> <p>Paths of the converted conda packages.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def convert_all(self) -&gt; list[Path]:\n    \"\"\"\n    Convert wheel to a conda package for every platform it supports.\n\n    Produces one conda package per conda platform (subdir) supported\n    by the wheel, each written into a `&lt;subdir&gt;/` subdirectory of the\n    output directory - conda package file names do not include the\n    platform, so packages for multiple platforms cannot share a\n    directory. A pure python or single-platform wheel produces a\n    single package (under `noarch/` or its platform subdirectory).\n\n    Returns:\n        Paths of the converted conda packages.\n    \"\"\"\n    packages: list[Path] = []\n    saved_out_dir = self.out_dir\n    saved_platform_tag = self.platform_tag\n    try:\n        for subdir, platform_tag in self._platform_groups().items():\n            self.out_dir = Path(saved_out_dir) / subdir\n            self.platform_tag = \"\" if platform_tag == \"any\" else platform_tag\n            packages.append(self.convert())\n    finally:\n        self.out_dir = saved_out_dir\n        self.platform_tag = saved_platform_tag\n    return packages\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter.read_metadata_file","level":5,"title":"read_metadata_file  <small><code>classmethod</code></small>","text":"<pre><code>read_metadata_file(file: Path) -&gt; email.message.Message\n</code></pre> <p>Read a wheel email-formatted metadata file (e.g. METADATA, WHEEL)</p> PARAMETER DESCRIPTION <code>file</code> <p>path to file</p> <p> TYPE: <code>Path</code> </p> RETURNS DESCRIPTION <code>Message</code> <p>Message object</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>@classmethod\ndef read_metadata_file(cls, file: Path) -&gt; email.message.Message:\n    \"\"\"\n    Read a wheel email-formatted metadata file (e.g. METADATA, WHEEL)\n\n    Args:\n        file: path to file\n\n    Returns:\n        Message object\n    \"\"\"\n    return email.message_from_string(\n        file.read_text(encoding=\"utf8\", errors=\"backslashreplace\"),\n        policy=email.policy.EmailPolicy(utf8=True, refold_source=\"none\"),  # type: ignore\n    )\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaConverter.translate_version_spec","level":5,"title":"translate_version_spec","text":"<pre><code>translate_version_spec(pip_version: str) -&gt; str\n</code></pre> <p>Convert a pip version spec to a conda version spec.</p> <p>Compatible release specs using the <code>~=</code> operator will be turned into two clauses using \"&gt;=\" and \"==\", for example <code>~=1.2.3</code> will become <code>&gt;=1.2.3,1.2.*</code>.</p> <p>Arbitrary equality clauses using the <code>===</code> operator will be converted to use <code>==</code>, but such clauses are likely to fail so a warning will be produced.</p> <p>Any leading \"v\" character in the version will be dropped. (e.g. <code>v1.2.3</code> changes to <code>1.2.3</code>).</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def translate_version_spec(self, pip_version: str) -&gt; str:\n    \"\"\"\n    Convert a pip version spec to a conda version spec.\n\n    Compatible release specs using the `~=` operator will be turned\n    into two clauses using \"&gt;=\" and \"==\", for example\n    `~=1.2.3` will become `&gt;=1.2.3,1.2.*`.\n\n    Arbitrary equality clauses using the `===` operator will be\n    converted to use `==`, but such clauses are likely to fail\n    so a warning will be produced.\n\n    Any leading \"v\" character in the version will be dropped.\n    (e.g. `v1.2.3` changes to `1.2.3`).\n    \"\"\"\n    pip_version = pip_version.strip()\n    version_specs = re.split(r\"\\s*,\\s*\", pip_version)\n    for i, spec in enumerate(version_specs):\n        if not spec:\n            continue\n        # spec for '~= &lt;version&gt;'\n        # https://packaging.python.org/en/latest/specifications/version-specifiers/#compatible-release\n        if m := pip_version_re.match(spec):\n            operator = m.group(\"operator\")\n            v = m.group(\"version\")\n            v = v.removeprefix(\"v\")\n            if operator == \"~=\":\n                # compatible operator, e.g. convert ~=1.2.3 to &gt;=1.2.3,==1.2.*\n                rv = m.group(\"release\")\n                rv_parts = rv.split(\".\")\n                operator = \"&gt;=\"\n                if len(rv_parts) &gt; 1:\n                    # technically ~=1 is not valid, but if we see it, turn it into &gt;=1\n                    v += f\",=={'.'.join(rv_parts[:-1])}.*\"\n            elif operator == \"===\":\n                operator = \"==\"\n                # TODO perhaps treat as an error in \"strict\" mode\n                self._warn(\n                    \"Converted arbitrary equality clause %s to ==%s - may not match!\",\n                    spec,\n                    v,\n                )\n            version_specs[i] = f\"{operator}{v}\"\n        else:\n            self._warn(\"Cannot convert bad version spec: '%s'\", spec)\n\n    return \",\".join(filter(bool, version_specs))\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.Wheel2CondaError","level":3,"title":"Wheel2CondaError","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Errors from Wheel2CondaConverter</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>class Wheel2CondaError(RuntimeError):\n    \"\"\"Errors from Wheel2CondaConverter\"\"\"\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter-functions","level":2,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.non_none_dict","level":3,"title":"non_none_dict","text":"<pre><code>non_none_dict(**kwargs: Any) -&gt; dict[str, Any]\n</code></pre> <p>dict that drops keys with None values</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def non_none_dict(**kwargs: Any) -&gt; dict[str, Any]:\n    \"\"\"dict that drops keys with None values\"\"\"\n    return {k: v for k, v in kwargs.items() if v is not None}\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/converter.html#whl2conda.api.converter.normalize_pypi_name","level":3,"title":"normalize_pypi_name","text":"<pre><code>normalize_pypi_name(name: str) -&gt; str\n</code></pre> <p>Normalize a PyPI package name per PEP 503.</p> <p>Converts to lowercase and replaces any runs of <code>[-_.]</code> with a single dash.</p> Source code in <code>src/whl2conda/api/converter.py</code> <pre><code>def normalize_pypi_name(name: str) -&gt; str:\n    \"\"\"Normalize a PyPI package name per PEP 503.\n\n    Converts to lowercase and replaces any runs of ``[-_.]``\n    with a single dash.\n    \"\"\"\n    return re.sub(r\"[-_.]+\", \"-\", name).lower()\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.converter"],"tags":[]},{"location":"reference/api/settings.html","level":1,"title":"whl2conda.settings","text":"<p>Settings for whl2conda.</p> <p>Internally, the settings are accessed through the settings global variable, which is an instance of Whl2CondaSettings</p> <p>The settings are loaded from a persistent JSON file, whose default location depends on the operating system, e.g.:</p> OS Path Windows <code>~/AppData/Local/whl2conda/whl2conda/settings.json</code> Linux <code>~/.config/whl2conda/settings.json</code> MacOS <code>~/Library/Application Support/whl2conda/settings.json</code> <p>The location of the settings file may also be overridden by <code>--settings</code> command line argument to <code>whl2conda</code>.</p> <p>Use the settings_file attribute to get the path used to load a given settings object.</p> <p>Settings may be viewed and modified at the command line using whl2conda config.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings-attributes","level":2,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.settings","level":3,"title":"settings  <small><code>module-attribute</code></small>","text":"<pre><code>settings = from_file()\n</code></pre> <p>User settings.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings-classes","level":2,"title":"Classes","text":"","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings","level":3,"title":"Whl2CondaSettings  <small><code>dataclass</code></small>","text":"<p>User settings for whl2conda.</p> <p>These are accessed through the global settings variable.</p> <p>The following settings are currently supported:</p> <ul> <li>auto_update_std_renames - whether to automatically update std rename mappings</li> <li>conda_format - default output conda package format</li> <li>pypi_indexes - table of known extra pypi indexes</li> </ul> Source code in <code>src/whl2conda/settings.py</code> <pre><code>@dataclasses.dataclass(kw_only=True)\nclass Whl2CondaSettings:\n    \"\"\"\n    User settings for whl2conda.\n\n    These are accessed through the global [settings][(m).] variable.\n\n    The following settings are currently supported:\n\n    * [auto_update_std_renames][.] - whether to automatically update std rename mappings\n    * [conda_format][.] - default output conda package format\n    * [pypi_indexes][.] - table of known extra pypi indexes\n\n    \"\"\"\n\n    SETTINGS_FILENAME: ClassVar[str] = \"settings.json\"\n    \"\"\"Default base filename for saved settings.\"\"\"\n\n    DEFAULT_SETTINGS_FILE: ClassVar[Path] = (\n        user_config_path(\"whl2conda\") / SETTINGS_FILENAME\n    )\n    \"\"\"Default filepath for saved settings.\"\"\"\n\n    # TODO:\n    #   - difftool\n    #   - pyproject defaults\n\n    auto_update_std_renames: _BoolField = _BoolField()\n    \"\"\"\n    Whether to automatically update the standard renames for operations\n    that need them. Default is false.\n    \"\"\"\n\n    conda_format: _CondaPackageFormatField = _CondaPackageFormatField()\n    \"\"\"\n    The default output conda package format if not specified. Default is V2.\n    \"\"\"\n\n    pypi_indexes: _SettingsField = _SettingsField(default=dict)\n    \"\"\"\n    Dictionary of aliases for pypi package indexes from which wheels can be\n    downloaded. Default is empty.\n    \"\"\"\n\n    #\n    # Internal attributes\n    #\n\n    _settings_file: Path = dataclasses.field(\n        default=DEFAULT_SETTINGS_FILE, compare=False\n    )\n    \"\"\"\n    Location of underlying settings file.\n    \"\"\"\n\n    _fieldnames: ClassVar[frozenset[str]] = frozenset()\n    \"\"\"\n    Set of public field names.\n    \"\"\"\n\n    @property\n    def settings_file(self) -&gt; Path:\n        \"\"\"\n        Settings file for this settings object.\n\n        Set from [from_file][..] constructor or else will be\n        [DEFAULT_SETTINGS_FILE][(c).].\n        \"\"\"\n        return self._settings_file\n\n    #\n    # Settings access/modification methods\n    #\n\n    def to_dict(self) -&gt; dict[str, Any]:\n        \"\"\"\n        Return dictionary containing public settings data.\n        \"\"\"\n        return {\n            k: v for k, v in dataclasses.asdict(self).items() if not k.startswith(\"_\")\n        }\n\n    def get(self, key: str) -&gt; Any:\n        \"\"\"\n        Get a value from the settings by string key.\n\n        The key may either be just the field name (e.g. 'conda-format')\n        or can refer to am entry within dictionary-valued field\n        (e.g. 'pypi-indexes.acme'). Note that the dashes in the first\n        component of the key will be converted to underscores.\n        \"\"\"\n        name, subkey = self._split_key(key)\n        value = getattr(self, name)\n\n        if subkey:\n            if not isinstance(value, dict):\n                raise KeyError(\n                    f\"Bad settings key '{key}': '{name}' is not a dictionary\"\n                )\n            if subkey not in value:\n                raise KeyError(f\"'{key}' is not set'\")\n            value = value[subkey]\n\n        return value\n\n    def set(self, key: str, value: Any) -&gt; None:\n        \"\"\"\n        Set a value in the settings by string key.\n\n        See [get][..] for details on key format.\n\n        This does not save the settings file.\n        \"\"\"\n        name, subkey = self._split_key(key)\n\n        if not subkey:\n            setattr(self, name, value)\n\n        else:\n            d = getattr(self, name)\n            if not isinstance(d, dict):\n                raise KeyError(\n                    f\"Bad settings key '{key}': '{name}' is not a dictionary\"\n                )\n            d[subkey] = value\n\n    def unset(self, key: str) -&gt; None:\n        \"\"\"\n        Unset attribute with given key.\n\n        The setting will revert to its original value.\n\n        See [get][..] for details on key format.\n\n        This does not save the settings file.\n        \"\"\"\n        name, subkey = self._split_key(key)\n\n        if not subkey:\n            delattr(self, name)\n\n        else:\n            d = getattr(self, name)\n            if not isinstance(d, dict):\n                raise KeyError(\n                    f\"Bad settings key '{key}': '{name}' is not a dictionary\"\n                )\n            try:\n                del d[subkey]\n            except KeyError:\n                pass\n\n    def unset_all(self) -&gt; None:\n        \"\"\"\n        Unset all settings, and revert to default values.\n        \"\"\"\n        for k in self._fieldnames:\n            self.unset(k)\n\n    #\n    # File operations\n    #\n\n    @classmethod\n    def from_file(cls, filename: Path | str = \"\") -&gt; Whl2CondaSettings:\n        \"\"\"\n        Return settings read from file.\n\n        Arguments:\n            filename: relative path to settings file (may start with '~')\n                defaults to [DEFAULT_SETTINGS_FILE][(c).] if not specified.\n        \"\"\"\n        settings = cls()\n        settings.load(filename or cls.DEFAULT_SETTINGS_FILE)\n        return settings\n\n    def load(self, filename: Path | str, reset_all: bool = False) -&gt; None:\n        \"\"\"\n        Reload settings from file\n\n        Args:\n            filename: relative path to settings file (may start with '~')\n            reset_all: if True, then all settings will be unset and reverted\n                to default value prior to loading.\n        \"\"\"\n        filepath = Path(Path(filename).expanduser())\n        self._settings_file = filepath\n        if reset_all:\n            self.unset_all()\n        if filepath.exists():\n            contents = filepath.read_text(\"utf8\")\n            json_obj = json.loads(contents)\n            for k, v in json_obj.items():\n                if k in self._fieldnames:\n                    try:\n                        setattr(self, k, v)\n                    except Exception as ex:\n                        print(\n                            f\"Cannot read '{k}' from '{filepath}': {ex}\",\n                            file=sys.stderr,\n                        )\n\n    def save(self, filename: Path | str = \"\") -&gt; None:\n        \"\"\"\n        Write settings to specified file in JSON format.\n\n        Args:\n            filename: file to write. Defaults to [settings_file][..]\n        \"\"\"\n        filepath = Path(filename or self._settings_file)\n        json_obj = self.to_dict()\n        json_obj[\"$whl2conda-version\"] = __version__\n        json_obj[\"$created\"] = str(dt.datetime.now(tz=dt.timezone.utc))\n        filepath.parent.mkdir(parents=True, exist_ok=True)\n        filepath.write_text(json.dumps(json_obj, indent=2))\n\n    #\n    # Internal methods\n    #\n\n    def _split_key(self, key: str) -&gt; tuple[str, str]:\n        parts = key.split(\".\", maxsplit=1)\n        name = _toidentifier(parts[0])\n        if name not in self._fieldnames:\n            raise KeyError(f\"Unknown settings key '{key}'\")\n        return name, parts[1] if len(parts) &gt; 1 else \"\"\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.DEFAULT_SETTINGS_FILE","level":5,"title":"DEFAULT_SETTINGS_FILE  <small><code>class-attribute</code></small>","text":"<pre><code>DEFAULT_SETTINGS_FILE: Path = (\n    user_config_path(\"whl2conda\") / SETTINGS_FILENAME\n)\n</code></pre> <p>Default filepath for saved settings.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.SETTINGS_FILENAME","level":5,"title":"SETTINGS_FILENAME  <small><code>class-attribute</code></small>","text":"<pre><code>SETTINGS_FILENAME: str = 'settings.json'\n</code></pre> <p>Default base filename for saved settings.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.auto_update_std_renames","level":5,"title":"auto_update_std_renames  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>auto_update_std_renames: _BoolField = _BoolField()\n</code></pre> <p>Whether to automatically update the standard renames for operations that need them. Default is false.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.conda_format","level":5,"title":"conda_format  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>conda_format: _CondaPackageFormatField = (\n    _CondaPackageFormatField()\n)\n</code></pre> <p>The default output conda package format if not specified. Default is V2.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.pypi_indexes","level":5,"title":"pypi_indexes  <small><code>class-attribute</code></small> <small><code>instance-attribute</code></small>","text":"<pre><code>pypi_indexes: _SettingsField = _SettingsField(default=dict)\n</code></pre> <p>Dictionary of aliases for pypi package indexes from which wheels can be downloaded. Default is empty.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.settings_file","level":5,"title":"settings_file  <small><code>property</code></small>","text":"<pre><code>settings_file: Path\n</code></pre> <p>Settings file for this settings object.</p> <p>Set from from_file constructor or else will be DEFAULT_SETTINGS_FILE.</p>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings-functions","level":4,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.from_file","level":5,"title":"from_file  <small><code>classmethod</code></small>","text":"<pre><code>from_file(filename: Path | str = '') -&gt; Whl2CondaSettings\n</code></pre> <p>Return settings read from file.</p> PARAMETER DESCRIPTION <code>filename</code> <p>relative path to settings file (may start with '~') defaults to DEFAULT_SETTINGS_FILE if not specified.</p> <p> TYPE: <code>Path | str</code> DEFAULT: <code>''</code> </p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>@classmethod\ndef from_file(cls, filename: Path | str = \"\") -&gt; Whl2CondaSettings:\n    \"\"\"\n    Return settings read from file.\n\n    Arguments:\n        filename: relative path to settings file (may start with '~')\n            defaults to [DEFAULT_SETTINGS_FILE][(c).] if not specified.\n    \"\"\"\n    settings = cls()\n    settings.load(filename or cls.DEFAULT_SETTINGS_FILE)\n    return settings\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.get","level":5,"title":"get","text":"<pre><code>get(key: str) -&gt; Any\n</code></pre> <p>Get a value from the settings by string key.</p> <p>The key may either be just the field name (e.g. 'conda-format') or can refer to am entry within dictionary-valued field (e.g. 'pypi-indexes.acme'). Note that the dashes in the first component of the key will be converted to underscores.</p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>def get(self, key: str) -&gt; Any:\n    \"\"\"\n    Get a value from the settings by string key.\n\n    The key may either be just the field name (e.g. 'conda-format')\n    or can refer to am entry within dictionary-valued field\n    (e.g. 'pypi-indexes.acme'). Note that the dashes in the first\n    component of the key will be converted to underscores.\n    \"\"\"\n    name, subkey = self._split_key(key)\n    value = getattr(self, name)\n\n    if subkey:\n        if not isinstance(value, dict):\n            raise KeyError(\n                f\"Bad settings key '{key}': '{name}' is not a dictionary\"\n            )\n        if subkey not in value:\n            raise KeyError(f\"'{key}' is not set'\")\n        value = value[subkey]\n\n    return value\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.load","level":5,"title":"load","text":"<pre><code>load(filename: Path | str, reset_all: bool = False) -&gt; None\n</code></pre> <p>Reload settings from file</p> PARAMETER DESCRIPTION <code>filename</code> <p>relative path to settings file (may start with '~')</p> <p> TYPE: <code>Path | str</code> </p> <code>reset_all</code> <p>if True, then all settings will be unset and reverted to default value prior to loading.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>def load(self, filename: Path | str, reset_all: bool = False) -&gt; None:\n    \"\"\"\n    Reload settings from file\n\n    Args:\n        filename: relative path to settings file (may start with '~')\n        reset_all: if True, then all settings will be unset and reverted\n            to default value prior to loading.\n    \"\"\"\n    filepath = Path(Path(filename).expanduser())\n    self._settings_file = filepath\n    if reset_all:\n        self.unset_all()\n    if filepath.exists():\n        contents = filepath.read_text(\"utf8\")\n        json_obj = json.loads(contents)\n        for k, v in json_obj.items():\n            if k in self._fieldnames:\n                try:\n                    setattr(self, k, v)\n                except Exception as ex:\n                    print(\n                        f\"Cannot read '{k}' from '{filepath}': {ex}\",\n                        file=sys.stderr,\n                    )\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.save","level":5,"title":"save","text":"<pre><code>save(filename: Path | str = '') -&gt; None\n</code></pre> <p>Write settings to specified file in JSON format.</p> PARAMETER DESCRIPTION <code>filename</code> <p>file to write. Defaults to settings_file</p> <p> TYPE: <code>Path | str</code> DEFAULT: <code>''</code> </p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>def save(self, filename: Path | str = \"\") -&gt; None:\n    \"\"\"\n    Write settings to specified file in JSON format.\n\n    Args:\n        filename: file to write. Defaults to [settings_file][..]\n    \"\"\"\n    filepath = Path(filename or self._settings_file)\n    json_obj = self.to_dict()\n    json_obj[\"$whl2conda-version\"] = __version__\n    json_obj[\"$created\"] = str(dt.datetime.now(tz=dt.timezone.utc))\n    filepath.parent.mkdir(parents=True, exist_ok=True)\n    filepath.write_text(json.dumps(json_obj, indent=2))\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.set","level":5,"title":"set","text":"<pre><code>set(key: str, value: Any) -&gt; None\n</code></pre> <p>Set a value in the settings by string key.</p> <p>See get for details on key format.</p> <p>This does not save the settings file.</p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>def set(self, key: str, value: Any) -&gt; None:\n    \"\"\"\n    Set a value in the settings by string key.\n\n    See [get][..] for details on key format.\n\n    This does not save the settings file.\n    \"\"\"\n    name, subkey = self._split_key(key)\n\n    if not subkey:\n        setattr(self, name, value)\n\n    else:\n        d = getattr(self, name)\n        if not isinstance(d, dict):\n            raise KeyError(\n                f\"Bad settings key '{key}': '{name}' is not a dictionary\"\n            )\n        d[subkey] = value\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.to_dict","level":5,"title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Return dictionary containing public settings data.</p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>def to_dict(self) -&gt; dict[str, Any]:\n    \"\"\"\n    Return dictionary containing public settings data.\n    \"\"\"\n    return {\n        k: v for k, v in dataclasses.asdict(self).items() if not k.startswith(\"_\")\n    }\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.unset","level":5,"title":"unset","text":"<pre><code>unset(key: str) -&gt; None\n</code></pre> <p>Unset attribute with given key.</p> <p>The setting will revert to its original value.</p> <p>See get for details on key format.</p> <p>This does not save the settings file.</p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>def unset(self, key: str) -&gt; None:\n    \"\"\"\n    Unset attribute with given key.\n\n    The setting will revert to its original value.\n\n    See [get][..] for details on key format.\n\n    This does not save the settings file.\n    \"\"\"\n    name, subkey = self._split_key(key)\n\n    if not subkey:\n        delattr(self, name)\n\n    else:\n        d = getattr(self, name)\n        if not isinstance(d, dict):\n            raise KeyError(\n                f\"Bad settings key '{key}': '{name}' is not a dictionary\"\n            )\n        try:\n            del d[subkey]\n        except KeyError:\n            pass\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/settings.html#whl2conda.settings.Whl2CondaSettings.unset_all","level":5,"title":"unset_all","text":"<pre><code>unset_all() -&gt; None\n</code></pre> <p>Unset all settings, and revert to default values.</p> Source code in <code>src/whl2conda/settings.py</code> <pre><code>def unset_all(self) -&gt; None:\n    \"\"\"\n    Unset all settings, and revert to default values.\n    \"\"\"\n    for k in self._fieldnames:\n        self.unset(k)\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.settings"],"tags":[]},{"location":"reference/api/stdrename.html","level":1,"title":"whl2conda.api.stdrename","text":"<p>Support for standard pypi to conda renames drawn from conda-forge.</p> <p>There are files generated automatically by conda-forge bots that include information about pypi/conda package names. These are available from:</p> <p>https://github.com/regro/cf-graph-countyfair/blob/master/mappings/pypi</p> <p>This package provides utility functions for downlaading mappings from that site and extracting a standard pypi to conda name mapping dictionary.</p> <p>NOTE: this module should not be considered stable! The API may change incompatibly in a future release.</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename-attributes","level":2,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DEFAULT_MIN_EXPIRATION","level":3,"title":"DEFAULT_MIN_EXPIRATION  <small><code>module-attribute</code></small>","text":"<pre><code>DEFAULT_MIN_EXPIRATION = 300\n</code></pre> <p>Default minimum expiration in seconds for cached renames</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.NAME_MAPPINGS_DOWNLOAD_URL","level":3,"title":"NAME_MAPPINGS_DOWNLOAD_URL  <small><code>module-attribute</code></small>","text":"<pre><code>NAME_MAPPINGS_DOWNLOAD_URL = (\n    f\"{RAW_MAPPINGS_URL}/{NAME_MAPPINGS_FILENAME}\"\n)\n</code></pre> <p>URL from which automatically generated pypi to conda name mappings are downloaded.</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename-classes","level":2,"title":"Classes","text":"","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DownloadedMappings","level":3,"title":"DownloadedMappings","text":"<p>               Bases: <code>NamedTuple</code></p> <p>Holds downloaded mapping table from github with HTTP headers.</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>class DownloadedMappings(NamedTuple):\n    \"\"\"\n    Holds downloaded mapping table from github with HTTP headers.\n    \"\"\"\n\n    url: str\n    headers: email.message.EmailMessage\n    mappings: Sequence[NameMapping]\n\n    @property\n    def date(self) -&gt; datetime.datetime | None:\n        \"\"\"Date from header\"\"\"\n        return parse_datetime(self.datestr)\n\n    @property\n    def datestr(self) -&gt; str:\n        \"\"\"Date string from header\"\"\"\n        return self.headers.get(\"Date\", \"\")\n\n    @property\n    def etag(self) -&gt; str:\n        \"\"\"ETag string from header\"\"\"\n        return self.headers.get(\"ETag\", \"\").strip('\"')\n\n    @property\n    def expires(self) -&gt; datetime.datetime | None:\n        \"\"\"Expires date string frome header\"\"\"\n        return parse_datetime(self.headers.get(\"Expires\", \"\"))\n\n    @property\n    def max_age(self) -&gt; int:\n        \"\"\"Max age from Cache-Control header\n\n        Max age in seconds from cache control header, or\n        else difference between [expires][..] and [date][..] or else -1.\n        \"\"\"\n        if cc := self.headers.get(\"Cache-Control\", \"\"):\n            if m := re.search(r\"max-age=(\\d+)\", cc):\n                return int(m.group(1))\n        if expires := self.expires:\n            date = self.date or datetime.datetime.now(datetime.timezone.utc)\n            return (expires - date).seconds\n        return -1\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DownloadedMappings-attributes","level":4,"title":"Attributes","text":"","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DownloadedMappings.date","level":5,"title":"date  <small><code>property</code></small>","text":"<pre><code>date: datetime | None\n</code></pre> <p>Date from header</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DownloadedMappings.datestr","level":5,"title":"datestr  <small><code>property</code></small>","text":"<pre><code>datestr: str\n</code></pre> <p>Date string from header</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DownloadedMappings.etag","level":5,"title":"etag  <small><code>property</code></small>","text":"<pre><code>etag: str\n</code></pre> <p>ETag string from header</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DownloadedMappings.expires","level":5,"title":"expires  <small><code>property</code></small>","text":"<pre><code>expires: datetime | None\n</code></pre> <p>Expires date string frome header</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.DownloadedMappings.max_age","level":5,"title":"max_age  <small><code>property</code></small>","text":"<pre><code>max_age: int\n</code></pre> <p>Max age from Cache-Control header</p> <p>Max age in seconds from cache control header, or else difference between expires and date or else -1.</p>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.NameMapping","level":3,"title":"NameMapping","text":"<p>               Bases: <code>TypedDict</code></p> <p>Expected format of github name_mapping.json table</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>class NameMapping(TypedDict):\n    \"\"\"Expected format of github name_mapping.json table\"\"\"\n\n    pypi_name: str\n    conda_name: str\n    import_name: str\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.NotModified","level":3,"title":"NotModified","text":"<p>               Bases: <code>HTTPError</code></p> <p>Indicates content was not modified</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>class NotModified(HTTPError):  # pylint: disable=too-many-ancestors\n    \"\"\"Indicates content was not modified\"\"\"\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename-functions","level":2,"title":"Functions","text":"","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.download_mappings","level":3,"title":"download_mappings","text":"<pre><code>download_mappings(\n    url: str = NAME_MAPPINGS_DOWNLOAD_URL,\n    *,\n    etag: str = \"\",\n    timeout: float = 20.0\n) -&gt; DownloadedMappings\n</code></pre> <p>Download pypi to conda name mappings from github</p> PARAMETER DESCRIPTION <code>url</code> <p>download url of mappings file on github</p> <p> TYPE: <code>str</code> DEFAULT: <code>NAME_MAPPINGS_DOWNLOAD_URL</code> </p> <code>etag</code> <p>ETag from previous download</p> <p> TYPE: <code>str</code> DEFAULT: <code>''</code> </p> <code>timeout</code> <p>max seconds to wait for connection</p> <p> TYPE: <code>float</code> DEFAULT: <code>20.0</code> </p> RETURNS DESCRIPTION <code>DownloadedMappings</code> <p>Mapping table and HTTP headers.</p> RAISES DESCRIPTION <code>NotModified</code> <p>if etag was specified and content has not changed</p> <code>HttpError</code> <p>other HTTP errors (e.g. 404 etc)</p> <code>URLError</code> <p>connection errors</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>def download_mappings(\n    url: str = NAME_MAPPINGS_DOWNLOAD_URL,\n    *,\n    etag: str = \"\",\n    timeout: float = 20.0,\n) -&gt; DownloadedMappings:\n    \"\"\"\n    Download pypi to conda name mappings from github\n\n    Args:\n        url: download url of mappings file on github\n        etag: ETag from previous download\n        timeout: max seconds to wait for connection\n\n    Returns:\n        Mapping table and HTTP headers.\n\n    Raises:\n        NotModified: if etag was specified and content has not changed\n        HttpError: other HTTP errors (e.g. 404 etc)\n        URLError: connection errors\n    \"\"\"\n\n    req = urllib.request.Request(\n        url,\n        headers={\"User-Agent\": f\"whl2conda/{__version__}\"},\n    )\n    if etag:\n        req.add_header(\"If-None-Match\", f'\"{etag}\"')\n\n    try:\n        with urllib.request.urlopen(req, timeout=timeout) as response:\n            headers = response.headers\n            content = response.read()\n            mappings = json.loads(content)\n    except HTTPError as err:\n        if err.status == HTTPStatus.NOT_MODIFIED:  # type: ignore\n            raise NotModified(\n                url,\n                err.code,\n                err.reason,\n                err.headers,\n                err.fp,\n            ) from err\n        raise\n\n    return DownloadedMappings(url, headers, mappings)\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.load_std_renames","level":3,"title":"load_std_renames","text":"<pre><code>load_std_renames(*, update: bool = False) -&gt; dict[str, str]\n</code></pre> <p>Load standard pypi to conda package rename table.</p> <p>A copy of this table is kept in a local a cache file (see user_stdrenames_path) The table will be read from that file, it it exists, otherwise the table included in this package will be copied to the user cache file.</p> PARAMETER DESCRIPTION <code>update</code> <p>if true, this will update the table from online list generated from conda-forge and saves it as the new cached copy.</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>dict[str, str]</code> <p>Dictionary of pypi to conda package name mappings. The</p> <code>dict[str, str]</code> <p>returned dictionary will also contain the entries \"$etag\",</p> <code>dict[str, str]</code> <p>\"$date\" and \"$source\" taken from the downloaded web file</p> <code>dict[str, str]</code> <p>from which it was computed.</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>def load_std_renames(\n    *,\n    update: bool = False,\n) -&gt; dict[str, str]:\n    \"\"\"\n    Load standard pypi to conda package rename table.\n\n    A copy of this table is kept in a local a cache\n    file (see [user_stdrenames_path][(m).])\n    The table will be read from that file, it it exists, otherwise the\n    table included in this package will be copied to the\n    user cache file.\n\n    Arguments:\n        update: if true, this will update the table from online\n            list generated from conda-forge and saves it as the\n            new cached copy.\n\n    Returns:\n        Dictionary of pypi to conda package name mappings. The\n        returned dictionary will also contain the entries \"$etag\",\n        \"$date\" and \"$source\" taken from the downloaded web file\n        from which it was computed.\n    \"\"\"\n    # Look for local copy of stdrenames\n    local_std_rename_file = user_stdrenames_path()\n    if not local_std_rename_file.exists():\n        resources = importlib.resources.files('whl2conda.api')\n        s = resources.joinpath(\"stdrename.json\").read_text(\"utf8\")\n        local_std_rename_file.parent.mkdir(parents=True, exist_ok=True)\n        local_std_rename_file.write_text(s, \"utf8\")\n\n    if update:\n        update_renames_file(local_std_rename_file)\n\n    s = local_std_rename_file.read_text(\"utf8\")\n    return json.loads(s)\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.parse_datetime","level":3,"title":"parse_datetime","text":"<pre><code>parse_datetime(s: str) -&gt; datetime.datetime | None\n</code></pre> <p>Parse datetime string from HTTP header</p> <p>Returns None if string is empty or time is malformed.</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>def parse_datetime(s: str) -&gt; datetime.datetime | None:\n    \"\"\"Parse datetime string from HTTP header\n\n    Returns None if string is empty or time is malformed.\n    \"\"\"\n    try:\n        return parsedate_to_datetime(s)\n    except Exception:  # pylint: disable=broad-exception-caught\n        return None\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.process_name_mapping_dict","level":3,"title":"process_name_mapping_dict","text":"<pre><code>process_name_mapping_dict(\n    mappings: DownloadedMappings,\n) -&gt; dict[str, str]\n</code></pre> <p>Convert name mapping table from github to simple rename table.</p> <p>This only returns mappings where the name is different.</p> PARAMETER DESCRIPTION <code>mappings</code> <p>downlaoded mappings</p> <p> TYPE: <code>DownloadedMappings</code> </p> RETURNS DESCRIPTION <code>dict[str, str]</code> <p>dictionary mapping pypi to conda package names</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>def process_name_mapping_dict(mappings: DownloadedMappings) -&gt; dict[str, str]:\n    \"\"\"\n    Convert name mapping table from github to simple rename table.\n\n    This only returns mappings where the name is different.\n\n    Args:\n        mappings: downlaoded mappings\n\n    Returns:\n        dictionary mapping pypi to conda package names\n    \"\"\"\n    renames: dict[str, str] = {\n        \"$source\": mappings.url,\n        \"$date\": mappings.datestr or formatdate(usegmt=True),\n        \"$etag\": mappings.etag,\n        \"$max-age\": str(max(mappings.max_age, DEFAULT_MIN_EXPIRATION)),\n    }\n    for entry in mappings.mappings:\n        pypi_name = entry.get(\"pypi_name\")\n        conda_name = entry.get(\"conda_name\")\n        if pypi_name and conda_name and pypi_name != conda_name:\n            renames[pypi_name] = conda_name\n    return renames\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.update_renames_file","level":3,"title":"update_renames_file","text":"<pre><code>update_renames_file(\n    renames_file: Path | str,\n    *,\n    url: str = NAME_MAPPINGS_DOWNLOAD_URL,\n    min_expiration: int = DEFAULT_MIN_EXPIRATION,\n    dry_run: bool = False\n) -&gt; bool\n</code></pre> <p>Update standard renames file from github if changed</p> <p>This will only download new data if the existing data has passed its expiration.</p> PARAMETER DESCRIPTION <code>renames_file</code> <p>path to renames file, which does not have to exist initially</p> <p> TYPE: <code>Path | str</code> </p> <code>url</code> <p>url of name mapping file to download. This file is expected to contain a JSON array of dictionary containing \"pypi_name\" and \"conda_name\" entries.</p> <p> TYPE: <code>str</code> DEFAULT: <code>NAME_MAPPINGS_DOWNLOAD_URL</code> </p> <code>min_expiration</code> <p>minimum seconds before existing data expires. Default is 5 minutes.</p> <p> TYPE: <code>int</code> DEFAULT: <code>DEFAULT_MIN_EXPIRATION</code> </p> <code>dry_run</code> <p>does not update the file, but still does download and returns True if file would change</p> <p> TYPE: <code>bool</code> DEFAULT: <code>False</code> </p> RETURNS DESCRIPTION <code>bool</code> <p>True if file was updated. False if file has not expired yet</p> <code>bool</code> <p>or upstream data has not changed.</p> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>def update_renames_file(\n    renames_file: Path | str,\n    *,\n    url: str = NAME_MAPPINGS_DOWNLOAD_URL,\n    min_expiration: int = DEFAULT_MIN_EXPIRATION,\n    dry_run: bool = False,\n) -&gt; bool:\n    \"\"\"\n    Update standard renames file from github if changed\n\n    This will only download new data if the existing\n    data has passed its expiration.\n\n    Args:\n        renames_file: path to renames file, which does not have to\n            exist initially\n        url: url of name mapping file to download. This file is\n            expected to contain a JSON array of dictionary\n            containing \"pypi_name\" and \"conda_name\" entries.\n        min_expiration: minimum seconds before existing data expires.\n            Default is 5 minutes.\n        dry_run: does not update the file, but still does download\n            and returns True if file would change\n\n    Returns:\n        True if file was updated. False if file has not expired yet\n        or upstream data has not changed.\n    \"\"\"\n    renames_path = Path(renames_file).expanduser()\n\n    etag = \"\"\n    if renames_path.is_file():\n        # check expiration information from existing file\n        current_renames = json.loads(renames_path.read_text(\"utf8\"))\n        if date := parse_datetime(current_renames.get(\"$date\", \"\")):\n            try:\n                max_age = int(current_renames.get(\"$max-age\", 0))\n            except Exception:  # pylint: disable=broad-exception-caught\n                max_age = 0\n            max_age = max(min_expiration, max_age)\n            if (date.timestamp() + max_age) &gt; time.time():\n                # Not expired yet\n                return False\n        etag = current_renames.get(\"$etag\")\n\n    try:\n        downloaded = download_mappings(url=url, etag=etag)\n    except NotModified:\n        return False\n\n    new_renames = process_name_mapping_dict(downloaded)\n    if not dry_run:\n        renames_path.parent.mkdir(parents=True, exist_ok=True)\n        renames_path.write_text(\n            json.dumps(new_renames, sort_keys=True, indent=2),\n            encoding=\"utf8\",\n        )\n\n    return True\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/api/stdrename.html#whl2conda.api.stdrename.user_stdrenames_path","level":3,"title":"user_stdrenames_path","text":"<pre><code>user_stdrenames_path() -&gt; Path\n</code></pre> <p>Path to user's cached copy of standard pypi to conda renames file</p> <p>The location of this file depends on the operating system:</p> <ul> <li>Linux: ~/.cache/whl2conda/stdrename.json</li> <li>MacOS: ~/Library/Caches/whl2conda/stdrename.json</li> <li>Windows: ~\\AppData\\Local\\whl2conda\\Cache\\stdrename.json</li> </ul> Source code in <code>src/whl2conda/api/stdrename.py</code> <pre><code>def user_stdrenames_path() -&gt; Path:\n    r\"\"\"Path to user's cached copy of standard pypi to conda renames file\n\n    The location of this file depends on the operating system:\n\n    * Linux: ~/.cache/whl2conda/stdrename.json\n    * MacOS: ~/Library/Caches/whl2conda/stdrename.json\n    * Windows: ~\\AppData\\Local\\whl2conda\\Cache\\stdrename.json\n    \"\"\"\n    return user_cache_path(\"whl2conda\").joinpath(\"stdrename.json\")\n</code></pre>","path":["Reference","API (not stable!)","whl2conda.api.stdrename"],"tags":[]},{"location":"reference/cli/whl2conda-build.html","level":1,"title":"whl2conda build","text":"","path":["Reference","Command line","whl2conda build"],"tags":[]},{"location":"reference/cli/whl2conda-build.html#usage","level":3,"title":"Usage","text":"<pre><code>usage: whl2conda build [-h] [--no-test] [-c CHANNELS] RECIPE_PATH\n</code></pre> <p>Build a conda package from a pure python wheel.</p> <p>This command is limited drop-in replacement for <code>conda build</code>. It requires that the conda recipe has a build script entry of the form <code>pip install</code> or <code>pip wheel</code>.</p> <p>This is an experimental feature and is still under active change and development.</p>","path":["Reference","Command line","whl2conda build"],"tags":[]},{"location":"reference/cli/whl2conda-build.html#positional-arguments","level":3,"title":"positional arguments","text":"<pre><code>  RECIPE_PATH\n</code></pre>","path":["Reference","Command line","whl2conda build"],"tags":[]},{"location":"reference/cli/whl2conda-build.html#options","level":3,"title":"options","text":"<pre><code>  -h, --help\n            show this help message and exit\n  --no-test\n  -c, --channel CHANNELS\n</code></pre>","path":["Reference","Command line","whl2conda build"],"tags":[]},{"location":"reference/cli/whl2conda-config.html","level":1,"title":"whl2conda config","text":"","path":["Reference","Command line","whl2conda config"],"tags":[]},{"location":"reference/cli/whl2conda-config.html#usage","level":3,"title":"Usage","text":"<pre><code>usage: whl2conda config [-h] [--generate-pyproject [&lt;dir-or-toml&gt;]]\n                        [--update-std-renames [&lt;file&gt;]] [--set &lt;key&gt; &lt;value&gt;]\n                        [--remove &lt;key&gt;] [--show [&lt;key&gt; ...] | --show-sources]\n                        [-n]\n</code></pre> <p>whl2conda configuration</p>","path":["Reference","Command line","whl2conda config"],"tags":[]},{"location":"reference/cli/whl2conda-config.html#options","level":3,"title":"options","text":"<pre><code>  -h, --help\n            show this help message and exit\n  --generate-pyproject [&lt;dir-or-toml&gt;]\n            Add default whl2conda tool entries to a pyproject file. \n            If argument is a directory entries will be added to \n            `pyproject.toml` in that directory. If argument ends\n            with suffix '.toml', that file will be updated. If\n            the argument is omitted or set to `out` the generated entry \n            will be written to stdout. Other values will result in an error.\n            This will create file if it does not already exist.\n            It will not overwrite existing entires.\n  --update-std-renames [&lt;file&gt;]\n            Update list of standard pypi to conda renames from internet and exit.\n            If a &lt;file&gt; is not named, the default copy will be updated at\n            /home/runner/.cache/whl2conda/stdrename.json.\n  -n, --dry-run\n            Do not write any files.\n</code></pre>","path":["Reference","Command line","whl2conda config"],"tags":[]},{"location":"reference/cli/whl2conda-config.html#user-settings-options","level":3,"title":"User settings options","text":"<pre><code>  These options allow you to view or modify persistent user settings\n  that affect the behavior of whl2conda. Note that all of these options\n  treat key dash and underscore in key names as equivalent (e.g. you\n  may use either `conda-format` or `conda_format`).\n\n  --set &lt;key&gt; &lt;value&gt;\n            Sets configuration parameter specified by &lt;key&gt; to given &lt;value&gt;.\n            For dictionary attributes (e.g. pypi-indexes), the &lt;key&gt; should be\n            of the form `&lt;key&gt;.&lt;entry&gt;` to set a specific entry in the table, e.g.\n\n               whl2conda config --set pypi-indexes.acme https://acme.com/pypi\n\n            Note that it is not currently possible to assign to entire dictionary.\n  --remove &lt;key&gt;\n            Unset user setting with given key. To remove an entry from a dictionary\n            use a key for the form '&lt;key&gt;.&lt;entry&gt;', e.g.\n\n              whl2conda config --remove pypi-indexes.acme\n  --show [&lt;key&gt; ...]\n            Show user settings from local settings file. If any &lt;key&gt; is given,\n            then only the specified settings will be displayed, otherwise\n            all settings will be shown.\n  --show-sources\n            The same as --show with no arguments but also displays location\n            of settings file.\n</code></pre>","path":["Reference","Command line","whl2conda config"],"tags":[]},{"location":"reference/cli/whl2conda-convert.html","level":1,"title":"whl2conda convert","text":"","path":["Reference","Command line","whl2conda convert"],"tags":[]},{"location":"reference/cli/whl2conda-convert.html#usage","level":3,"title":"Usage","text":"<pre><code>usage: whl2conda convert &lt;wheel&gt; [options]\n       whl2conda convert [&lt;project-root&gt;] [options]\n</code></pre> <p>Generates a conda package from a pure python wheel</p>","path":["Reference","Command line","whl2conda convert"],"tags":[]},{"location":"reference/cli/whl2conda-convert.html#input-options","level":3,"title":"Input options","text":"<pre><code>  [&lt;wheel&gt; | &lt;project-root&gt;]\n            Either path to a wheel file to convert or a project root\n            directory containing a pyproject.toml or setup.py file.\n  --project-root, --root &lt;dir&gt;\n            Project root directory. This is a directory containing either a\n            pyproject.toml or a (deprecated) setup.py file. This option may\n            not be used if the project directory was given as the positional\n            argument.\n\n            If not specified, the project root will be located by searching\n            the wheel directory and its parent directories, or if no wheel\n            given, will default to the current directory.\n  --from-pypi &lt;package-spec&gt;\n            Download package satisfying &lt;package-spec&gt; from standard pypi.org repository.\n  --from-index &lt;index-url&gt; &lt;package-spec&gt;\n            Download package satisfying &lt;package-spec&gt; from repository at &lt;index-url&gt;.\n  -w, --wheel-dir &lt;dir&gt;\n            Location of wheel directory. Defaults to dist/ subdirectory of \n            project.\n  --ignore-pyproject\n            Ignore settings from pyproject.toml file, if any\n  --update-stdrenames, --no-update-stdrenames\n            Whether to update list of standard pypi to conda renames from internet. \n            Default from settings: False\n  --allow-metadata-version &lt;version&gt;\n            Allow specified (unsupported) metadata version in input wheel files.\n</code></pre>","path":["Reference","Command line","whl2conda convert"],"tags":[]},{"location":"reference/cli/whl2conda-convert.html#output-options","level":3,"title":"Output options","text":"<pre><code>  --out-dir, --out &lt;dir&gt;\n            Output directory for conda package. Defaults to wheel directory\n            or else project dist directory.\n  --overwrite\n            Overwrite existing output files.\n  --format, --out-format {V1,tar.bz2,V2,conda,tree}\n            Output package format (None)\n  --build-wheel\n            Build wheel\n  --build-number BUILD_NUMBER\n            Specify build number. Otherwise taken from wheel.\n</code></pre>","path":["Reference","Command line","whl2conda convert"],"tags":[]},{"location":"reference/cli/whl2conda-convert.html#override-options","level":3,"title":"Override options","text":"<pre><code>  --name &lt;package-name&gt;\n            Override package name\n  -R, --dependency-rename &lt;pip-name&gt; &lt;conda-name&gt;\n            Rename pip dependency for conda. May be specified muliple times.\n  -A, --add-dependency &lt;conda-dep&gt;\n            Add an additional conda dependency. May be specified multiple times.\n  -D, --drop-dependency &lt;pip-name&gt;\n            Drop dependency with given name from conda dependency list.\n            May be specified multiple times.\n  -K, --keep-pip-dependencies\n            Retain pip dependencies in python dist_info of conda package.\n  --python &lt;version-spec&gt;\n            Set/override python dependency.\n</code></pre>","path":["Reference","Command line","whl2conda convert"],"tags":[]},{"location":"reference/cli/whl2conda-convert.html#binary-conversion-options","level":3,"title":"Binary conversion options","text":"<pre><code>  --allow-impure\n            Allow conversion of non-pure python wheels containing\n            binary extensions into platform-specific conda packages.\n            Generates tight Python version pins and OS constraints from wheel\n            tags. Stable ABI (abi3) wheels are only pinned to a minimum python\n            version. Use --python to override the generated python dependency.\n  --for-conda-forge, --for-cpython\n            When converting a stable ABI (abi3) wheel, also add the CEP-20\n            python pins used by conda-forge: 'cpython' (excludes PyPy) and\n            '_python_abi3_support' (excludes free-threaded builds). These\n            packages only exist on the conda-forge channel, so do not use\n            this option for packages targeting channels without them.\n  --platform-tag &lt;tag&gt;\n            Wheel platform tag to convert for, when converting a\n            multi-platform (\"fat\") binary wheel that supports several\n            (e.g. 'macosx_10_15_x86_64'). By default, the platform\n            matching the current system is preferred.\n  --all-platforms\n            Generate a conda package for every platform supported by\n            the wheel, each written into a &lt;subdir&gt;/ subdirectory of\n            the output directory (e.g. osx-arm64/).\n  --download-platform &lt;tag&gt;\n            Target platform tag for download (e.g. 'manylinux2014_x86_64',\n            'macosx_11_0_arm64', 'win_amd64'). Used with --from-pypi or\n            --from-index to download a platform-specific binary wheel.\n            Implies --allow-impure.\n  --download-python-version &lt;ver&gt;\n            Target Python version for download (e.g. '3.12').\n            Used with --from-pypi or --from-index.\n  --download-abi &lt;tag&gt;\n            Target ABI tag for download (e.g. 'cp312').\n            Used with --from-pypi or --from-index.\n</code></pre>","path":["Reference","Command line","whl2conda convert"],"tags":[]},{"location":"reference/cli/whl2conda-convert.html#help-and-debug-options","level":3,"title":"Help and debug options","text":"<pre><code>  -n, --dry-run\n            Do not write any files.\n  --batch, --not-interactive\n            Batch mode - disable interactive prompts.\n  --yes     Answer 'yes' or choose default to all interactive questions\n  -v, --verbose\n            Increase verbosity.\n  -q, --quiet\n            Less verbose output\n  -h, -?, --help\n            Show usage and exit.\n</code></pre>","path":["Reference","Command line","whl2conda convert"],"tags":[]},{"location":"reference/cli/whl2conda-diff.html","level":1,"title":"whl2conda diff","text":"","path":["Reference","Command line","whl2conda diff"],"tags":[]},{"location":"reference/cli/whl2conda-diff.html#usage","level":3,"title":"Usage","text":"<pre><code>usage: whl2conda diff [-h] [-T &lt;tool&gt;] [-A ...] [--json] [--all] [--strict]\n                      [--ignore &lt;category&gt;] [--run-export &lt;name&gt;]\n                      &lt;package1&gt; &lt;package2&gt;\n</code></pre> <p>Compare the content of two conda packages</p> <p>By default, this semantically analyzes the differences between the two packages and reports notable and unexpected differences, suppressing differences that are expected when comparing a package generated by this tool against an equivalent package built from a recipe (e.g. from conda-forge). Exits with a non-zero status if any unexpected differences are found.</p> <p>Alternatively, if a diff tool is specified with -T, this will unpack each conda package into temporary directories, normalize the layout of files to minimize line differences, and run the specified diff tool and arguments.</p>","path":["Reference","Command line","whl2conda diff"],"tags":[]},{"location":"reference/cli/whl2conda-diff.html#positional-arguments","level":3,"title":"positional arguments","text":"<pre><code>  &lt;package1&gt;\n            First package to compare\n  &lt;package2&gt;\n            Second package to compare\n</code></pre>","path":["Reference","Command line","whl2conda diff"],"tags":[]},{"location":"reference/cli/whl2conda-diff.html#options","level":3,"title":"options","text":"<pre><code>  -h, --help\n            show this help message and exit\n  -T, --diff-tool &lt;tool&gt;\n            Diff tool to use instead of semantic analysis.\n\n            The tool is expected to take positional arguments\n            for each directory. Additional arguments may be\n            passed after --args.\n  -A, --args ...\n            All remaining arguments passed to diff tool\n</code></pre>","path":["Reference","Command line","whl2conda diff"],"tags":[]},{"location":"reference/cli/whl2conda-diff.html#analysis-options","level":3,"title":"Analysis options","text":"<pre><code>  These options apply to semantic analysis (no -T).\n\n  --json    Write analysis results as JSON instead of a text report\n  --all     Also report expected differences\n  --strict  Treat notable differences as errors\n  --ignore &lt;category&gt;\n            Ignore differences in the given category. May be repeated.\n            Categories: package-name, package-version, build-string, build-number, noarch-vs-platform, license, about-field, timestamp, dep-missing, dep-extra, dep-run-export, dep-version, dep-unrenamed, python-pin, file-missing, file-extra, file-content, binary-content, pycache, entry-point, dist-info-metadata, dist-info-other, info-extra-file\n  --run-export &lt;name&gt;\n            Additional dependency name to treat as a benign run-export\n            when only present in the second package. May be repeated.\n</code></pre>","path":["Reference","Command line","whl2conda diff"],"tags":[]},{"location":"reference/cli/whl2conda-install.html","level":1,"title":"whl2conda install","text":"","path":["Reference","Command line","whl2conda install"],"tags":[]},{"location":"reference/cli/whl2conda-install.html#usage","level":3,"title":"Usage","text":"<pre><code>usage: whl2conda install (-p &lt;env-path&gt; | -n &lt;env-name&gt;) &lt;package-file&gt;... [options]\n       whl2conda install --conda-bld &lt;package-file&gt; [options]\n</code></pre> <p>Install conda package files</p> <p>This can be used to install one or more conda package files (generated by <code>whl2conda build</code>) either into a conda environment (for testing) or into your local conda build directory.</p>","path":["Reference","Command line","whl2conda install"],"tags":[]},{"location":"reference/cli/whl2conda-install.html#positional-arguments","level":3,"title":"positional arguments","text":"<pre><code>  &lt;package-file&gt;\n            Conda package file to be installed\n            Must have extension .conda or .tar.bz2\n</code></pre>","path":["Reference","Command line","whl2conda install"],"tags":[]},{"location":"reference/cli/whl2conda-install.html#options","level":3,"title":"options","text":"<pre><code>  -h, --help\n            show this help message and exit\n</code></pre>","path":["Reference","Command line","whl2conda install"],"tags":[]},{"location":"reference/cli/whl2conda-install.html#target-choose-one","level":3,"title":"Target (choose one)","text":"<pre><code>  -p, --prefix &lt;env-path&gt;\n            Path to target conda environment\n  -n, --name &lt;env-name&gt;\n            Name of target conda enviroment\n  --conda-bld\n            Install into local conda-bld\n</code></pre>","path":["Reference","Command line","whl2conda install"],"tags":[]},{"location":"reference/cli/whl2conda-install.html#environment-options","level":3,"title":"Environment options","text":"<pre><code>  These options can be used with -n/-p when install into\n  a conda environment. They are otherwise ignored.\n\n  --create  Create environment if it does not exist.\n  --only-deps\n            Only install package dependencies, not the package itself.\n  --no-deps\n            Only packages themselves without any dependencies.\n  --mamba   Use mamba instead of conda for install actions\n  --extra ...\n            All the remaining arguments after this flat will be passed\n            to `conda install` or `conda create`. This can be used to add\n            additional dependencies for testing.\n</code></pre>","path":["Reference","Command line","whl2conda install"],"tags":[]},{"location":"reference/cli/whl2conda-install.html#common-options","level":3,"title":"Common options","text":"<pre><code>  --dry-run\n            Display operations but don't actually install\n  --yes     Answer yes to prompts.\n</code></pre>","path":["Reference","Command line","whl2conda install"],"tags":[]},{"location":"reference/cli/whl2conda.html","level":1,"title":"whl2conda","text":"","path":["Reference","Command line","whl2conda"],"tags":[]},{"location":"reference/cli/whl2conda.html#usage","level":3,"title":"Usage","text":"<pre><code>usage: whl2conda [options] &lt;command&gt; ...\n</code></pre> <p>Utility for building and testing conda package generated directly from a python wheel.</p> <p>See <code>whl2conda build --help</code> for more information.</p>","path":["Reference","Command line","whl2conda"],"tags":[]},{"location":"reference/cli/whl2conda.html#options","level":3,"title":"options","text":"<pre><code>  -h, --help\n            show this help message and exit\n  --settings &lt;filepath&gt;\n            Override default settings file\n  --version\n            show program's version number and exit\n</code></pre>","path":["Reference","Command line","whl2conda"],"tags":[]},{"location":"reference/cli/whl2conda.html#commands","level":3,"title":"Commands","text":"<pre><code>  &lt;command&gt;\n    build   conda-build replacement\n    config  configure whl2conda\n    convert\n            builds a conda package from a python wheel\n    diff    compare contents of conda packages\n    install\n            install conda package file with dependencies\n</code></pre>","path":["Reference","Command line","whl2conda"],"tags":[]}]};