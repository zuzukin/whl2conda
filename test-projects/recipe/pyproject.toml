[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "hello"
version = "1.0.0"
description = "whl2conda build test fixture"
license = { text = "MIT" }
requires-python = ">=3.9"

[tool.hatch.build.targets.wheel]
packages = ["hello"]
